/**
 * Toy GI namespace for 0.5: typelib + stub {@code .so} that RPCs {@link ping}.
 *
 * Stock GJS loads this ahead of mutter via {@code GI_TYPELIB_PATH}.
 *
 * == Example ==
 *
 * {{{
 * const GiRpcSmoke = imports.gi.GiRpcSmoke;
 * print(GiRpcSmoke.ping('hello'));
 * }}}
 */
namespace GiRpcSmoke
{
}

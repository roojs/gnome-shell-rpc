		/*
		 * style-changed is a generated virtual signal. Do not subscribe
		 * here — nested RPC mid-{@code .new} reply parse. Actor construct
		 * still subscribes after mint (Phase 2 removes that).
		 */

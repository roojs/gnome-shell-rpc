		/**
		 * ActorMeta {@code enabled} — parent is size-locked C GType (no Vala
		 * props). GJS {@code TouchSwipeGesture} / SwipeTracker
		 * {@code bind_property('enabled', …)}.
		 */
		public bool enabled { get; set construct; default = true; }

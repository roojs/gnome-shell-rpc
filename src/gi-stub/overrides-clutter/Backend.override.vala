	/**
	 * Shell {@code InputMethod} is a client GJS subclass — no {@code rpc_lid}.
	 * Stock is in-process; generated RPC stubs abort on the pointer. Keep local.
	 */
	public InputMethod? input_method { get; set; }

/**
 * Dummy RPC server for the 0.5 GiRpcSmoke toy.
 *
 * Prints each ping and echoes {@code echo:} + msg. Not mutter.
 *
 * == Example ==
 *
 * {{{
 * ./build/src/gi-rpc-echo --debug
 * }}}
 */
namespace GnomeShellRpc.GiRpcEcho
{
}

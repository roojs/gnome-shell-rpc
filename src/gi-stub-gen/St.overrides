# gi-stub-gen overrides for St-16 (Type.method key=value).

# Emit GIR implements on stub classes (Viewport → Scrollable). Deny an iface
# name to skip it; Clutter/Meta leave this off until Animatable/etc. are ready.
# Foreign: Type.implements=Clutter.Animatable (see Adjustment).
Namespace emit_implements=1

# Stock St.Adjustment implements Clutter.Animatable (find_property / ease).
Adjustment implements=Clutter.Animatable

# Stock St.ImageContent implements Clutter.Content (Actor.content).
ImageContent implements=Clutter.Content

# Class vfunc slots (Button.clicked/transition, Widget.get_focus_chain, …).
Namespace emit_class_slots=1
# Slot/signal clash → keep GIR signal (rename slot *_vfunc). Exact names.
Namespace signal_prefer=clicked
Namespace signal_prefer=long_press
Namespace signal_prefer=repaint
Namespace signal_prefer=popup_menu
Namespace signal_prefer=style_changed
Namespace signal_prefer=primary_icon_clicked
Namespace signal_prefer=secondary_icon_clicked

Widget.get_focus_chain list_elem=Widget

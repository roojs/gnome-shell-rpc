/**
 * Throwaway GTK top bar for plan 0.3 — not the product shell.
 *
 * {@link Session} / {@link Display} / {@link Window} are the Vala RPC proxies
 * for this binary only. GJS Meta stubs live in {@code src/meta-mini/}.
 *
 * == Example ==
 *
 * {{{
 * ./build/src/fake-shell --debug
 * }}}
 */
namespace GnomeShellRpc.FakeShell
{
}

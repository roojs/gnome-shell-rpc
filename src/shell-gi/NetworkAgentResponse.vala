/**
 * Stock {@code ShellNetworkAgentResponse} — {@code shell-network-agent.h}.
 */
namespace Shell
{
	public enum NetworkAgentResponse
	{
		CONFIRMED = 0,
		USER_CANCELED = 1,
		INTERNAL_ERROR = 2
	}
}

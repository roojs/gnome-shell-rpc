/* Stock shell_app_system_search — nested GStrv for the typelib (gtk-doc).
 * gsr_app_system_search_groups is the Vala [CCode] extern only. */
#pragma once

#include <glib.h>

G_BEGIN_DECLS

char *** shell_app_system_search (const char *search_string);
char *** gsr_app_system_search_groups (const char *search_string);

G_END_DECLS

# 0.7.8 — GI RPC mock server (fast client import tests)

**User goal:** nested **gnome-shell boots**. Everything else is a means — only do it if it unblocks boot.

Checklist: `docs/guide-to-writing-plans.md`.

**Status:** **IN PROGRESS** — Phase B landed; Phase C/D in [`mock import-graph audit`](../bugs/2026-09-04-0.7.8-mock-import-graph-audit.md)

**Parent:** [`0.7.7-thin-shell-bootstrap.md`](0.7.7-thin-shell-bootstrap.md)

**Blocks:** [mock import-graph audit](../bugs/2026-09-04-0.7.8-mock-import-graph-audit.md) (supersedes [0.7.7 nested bug](../bugs/2026-09-03-0.7.7-main-import-double-free.md))

**Depends on:** [`0.5-gnome-shell-js-layer.md`](0.5-gnome-shell-js-layer.md) `gi-rpc-echo` **✔️**; client stubs + `Runtime.register` **✔️**; **OLLMchat** [`RPC-1.7-DONE-mock-dispatch-and-gi-mock.md`](../../../gitlive/OLLMchat/docs/plans/done/RPC-1.7-DONE-mock-dispatch-and-gi-mock.md) **✔️**

---

## Where we are

**Partway to the bug.** Repro works; crash pinned to `messageList.js` (notification messages). Exact broken stub not found yet.

**Run:** `./scripts/gi-rpc-smoke.sh run` · **Trace:** `GI_RPC_REGISTER_CLASS_TRACE=1` + same

Details: [bug report](../bugs/2026-09-04-0.7.8-mock-import-graph-audit.md).

---

## Purpose

Run **import / `GObject.registerClass` smokes** without nested Mutter.

**🔷** Thin `gi-rpc-mock` executable: socket + **Ffi** (`RPC-Daemon`, …) + **`Gi.register`** + **`Request.register_mock(HelperMock)`**.

**🚫** Real `mutter-rpc` never calls `register_mock`. Not a compositor substitute.

---

## How libocrpc mock mode works (shipped RPC-1.7)

**Calling `register_mock` switches the server into mock mode.** You do **not** implement or call `GiMock` yourself — it is internal to libocrpc.

```
Ffi  →  RPC-Daemon, RPC-Live-Callback.*, …
Helper  →  your MockDispatch object (return true if handled)
GiMock  →  automatic when helper returns false (GIR typed empties, no C invoke)
```

**Real compositor** (no `register_mock`):

```
Ffi  →  …
Gi   →  real g_function_info_invoke
```

**Your job in this repo:** one **`HelperMock`** class — `switch (request.method)` for `Helper-*`, `RPC-Bootstrap.*`, etc. Return **`true`** + `reply()` when matched. Return **`false`** for everything else → libocrpc runs **`GiMock`** for you.

---

## Architecture

```
gi-rpc-mock                         gnome-shell-rpc / gjs-embed
───────────                         ───────────────────────────
Gi.register(Meta|Clutter|St)   ←→   stub .so → Runtime.call_values
Request.register(RPC-Daemon)        import panel.js / registerClass
Request.register_mock(HelperMock)
  true  → Helper switch handles call
  false → GiMock (inside Request.dispatch)
```

| Call kind | Handler |
| --- | --- |
| `RPC-Daemon.*`, `RPC-Live-Callback.*` | **Ffi** |
| `Helper-*`, `RPC-Bootstrap.*`, … | **`HelperMock.dispatch`** — return **true** |
| `Meta-*`, `St-*`, `Clutter.*`, … | **`GiMock`** (libocrpc) — helper returns **false** |

Boot order: **`Gi.register("Meta"|"Clutter"|"St", "16")`** then **`register_mock`** (GiMock uses same `Gi.types` / `namespaces` lookup as real Gi).

---

## Phase A — libocrpc **✔️**

Shipped: [`RPC-1.7-DONE-mock-dispatch-and-gi-mock.md`](../../../gitlive/OLLMchat/docs/plans/done/RPC-1.7-DONE-mock-dispatch-and-gi-mock.md).

---

## Phase B — `gi-rpc-mock` **✔️**

**Add:** `src/gi-rpc-mock/` (mirror `gi-rpc-echo/`)

| File | Role |
| --- | --- |
| `Application.vala` | `Gi.register` ×3; socket; `Request.register(RPC-Daemon)`; `Request.register_mock(new HelperMock())`; `live_handles = true` |
| `HelperMock.vala` | `MockDispatch` — `switch (request.method)`; **true** + reply for Helpers; **false** for all GI-shaped calls |

**🚫** Do **not** call `new GiMock(...)` or `Gi.dispatch` from consumer code.

**meson:** `gi_rpc_smoke` option or `-Dgi_rpc_mock=enabled`.

---

## Phase C — Wire smokes **🔷** **⏳**

```bash
./scripts/gi-rpc-smoke.sh run
# or: server / client split — see scripts/gi-rpc-smoke.sh --help
# module trace: GI_MAIN_SMOKE_MODULE=panel.js ./scripts/gi-rpc-smoke.sh run src/gjs-embed/register-class-trace-smoke.js
```

**ℹ️** Unknown Helper → log method; add one `switch` arm in `HelperMock.vala`.  
**Bug tracker:** [`2026-09-04-0.7.8-mock-import-graph-audit.md`](../bugs/2026-09-04-0.7.8-mock-import-graph-audit.md)  
**⏳ Prerequisite bug:** [`2026-09-04-0.7.8-mock-smoke-terminate-exit.md`](../bugs/2026-09-04-0.7.8-mock-smoke-terminate-exit.md) — smokes must exit in &lt; 2 s before bisect loops are usable.

---

## Phase D — Coverage bar **🔷** **⏳**

| Smoke | Pass bar |
| --- | --- |
| `./scripts/gi-rpc-smoke.sh run` | Real startup hits memory bug on mock — **done** |
| `GI_RPC_REGISTER_CLASS_TRACE=1` + same | Last class before crash pinned — **done** (`messageList.js`) |
| Fix + retest | No `double free` on mock; then confirm nested |

**🚫** Mock pass ≠ nested boot — rerun `mutter-rpc --nested` before closing import bugs.

---

## Out of scope **🚫**

- Using `register_mock` on real compositor
- Consumer `GiMock` / manual mock dispatch
- JSON fixtures
- Replacing nested tests for S1+ boot

# 1.0 — Run to the end (completeness → restartable shell)

**User goal:** stock shell JS keeps running on a **complete enough** GI
surface; **Shell** is owned Vala (not `libshell` C); Meta/Clutter/St TEMP
denies are closed; stack is a **host-session candidate** (restart shell,
compositor stays).

**Status:** **BACKLOG** until **0.8** ✅ (or user parallel-tracks). Active
plan today: [`0.8-init-complete-and-interaction.md`](0.8-init-complete-and-interaction.md).

Checklist: `docs/guide-to-writing-plans.md`.

**🚫** Vendor `js/` / `GI_RPC_JS_OVERRIDE_DIR` as production · host
`meson install` without ask · OLLMchat edits without FAIL gate there.

---

## Phase 0 — Finish 0.8

**🔷** `⏳` Close parked 0.8 chrome so this plan owns the backlog.

**Prove:** `./scripts/weston-gsr-prove.sh` still greens A/B; placement looks
right in `./scripts/weston-gsr-session.sh`.

| # | Work | Status | Notes |
| - | ---- | ------ | ----- |
| 0.1 | Notification / menu / panel bold placement | ⏳ | [`bugs/2026-09-15-gjs-layoutmanager-null-allocate.md`](../bugs/2026-09-15-gjs-layoutmanager-null-allocate.md) |
| 0.2 | Soft Event typelib packing / seat pick focus | soft | only if chrome still lies |
| 0.3 | Mark **0.8** ✅; make **1.0** active (banner moves here) | ⏳ | user call |

**🚫** No status-theatre banner on this file until it is the active plan.

**Done when:** 0.1–0.3 done or explicitly parked with a dated reason.

---

## Phase S — Shell Vala (hand C → Vala)

**🔷** `⏳` Finish `Shell-16` in `src/shell-gi/` by hand.

**How:** read stock C under `vendor/gnome-shell/src/shell-*.c` (or distro
sources if vendor lacks a file), rewrite in Vala (usually smaller), **RPC
relay only where compositor state is required** (same pattern as
`GLSLEffect`). GIR truth: `/usr/share/gnome-shell/Shell-16.gir`.

**🚫** No Shell generator. **🚫** Invented APIs — match stock names/roles.

**Already owned:** classes `App`, `AppSystem`, `AppUsage`, `CameraMonitor`,
`Global`, `GLSLEffect`, `Screenshot`, `SquareBin`, `Stack`, `WM`, `WindowPreview`,
`WindowPreviewLayout`, `WindowTracker`, `WorkspaceBackground`, `BlurEffect`,
`NetworkAgent`, `MountOperation`, `PolkitAuthenticationAgent`, `SecureTextBuffer`,
`KeyringPrompt`, `InvertLightnessEffect`, `PerfLog`; enums `ActionMode`,
`AppState`, `AppLaunchGpu`, `BlurMode`, `NetworkAgentResponse`; most `util_*` in
`Util.vala` (incl. systemd async + finish, `util_wifexited`).

**Prove:** rebuild `libshell-gi-16` + typelib; nest boots
(`./scripts/weston-gsr-prove.sh`). Logs:
`~/.cache/gnome-shell-rpc/{org.gnome.ShellRpc,mutter-rpc}.debug.log`.

**Add:** `src/shell-gi/{Type}.vala` (+ Helper only if compositor must run
the body). Tick **✔️** per row. Work **top → bottom** — rows marked
*with …* wait until that class lands.

### Priority notes (HEAD ~2026-08 + Ubuntu 48)

| Item | Note |
| ---- | ---- |
| `networkAgent` / `polkitAgent` / `keyring` / automount | **User-session components** in `sessionMode.js` — high for a real desktop; Network follows Blur |
| `Stack` / `BlurEffect` / `WindowPreviewLayout` | Stock chrome (unlock, overview, app grid) |
| `InvertLightnessEffect` | Magnifier a11y — **low** (after session agents) |
| `PerfLog` + dump helpers | Looking-glass / `scripting.js` only — low |
| `TrayIcon` / `TrayManager` | **Legacy XEmbed tray** — no stock JS callers; Ubuntu `ubuntu-appindicators` still uses `Shell.TrayManager` — low for core path |
| `QrCodeGenerator` | In this distro GIR; **absent** from upstream HEAD `src/` — HEAD QR UI uses `GnomeQR`, not Shell — skip / last |

### Worklist

| # | Symbol | Depends / with | Why here | Status |
| - | ------ | -------------- | -------- | ------ |
| S.1 | `util_start_systemd_unit_finish` | async already owned | Vala `async` emits finish (GIR ✔️) | ✔️ |
| S.2 | `util_stop_systemd_unit_finish` | async already owned | same | ✔️ |
| S.3 | `util_systemd_unit_exists_finish` | async already owned | same | ✔️ |
| S.4 | `AppLaunchGpu` | `App` already owned | `AppLaunchGpu.vala` | ✔️ |
| S.5 | `util_regex_escape` | — | `Util.vala` | ✔️ |
| S.6 | `util_get_translated_folder_name` | — | desktop-directories lookup | ✔️ |
| S.7 | `util_check_cloexec_fds` | — | `/proc/self/fd` walk | ✔️ |
| S.8 | `util_create_pixbuf_from_data` | — | `Gdk.Pixbuf.from_data` | ✔️ |
| S.9 | `Stack` | — | `Stack.vala` (stock shell-stack) | ✔️ |
| S.10 | `WindowPreviewLayout` | — | overview window peeks | ✔️ |
| S.11 | `BlurMode` | **with** `BlurEffect` | enum on blur API | ✔️ |
| S.12 | `BlurEffect` | S.11 | unlock dialog blur | ✔️ |
| S.13 | `NetworkAgentResponse` | **with** `NetworkAgent` | respond() args | ✔️ |
| S.14 | `util_wifexited` | **with** `NetworkAgent` | `networkAgent.js` child exit | ✔️ |
| S.15 | `NetworkAgent` | S.13–S.14 | `networkAgent` component (NM) | ✔️ |
| S.16 | `MountOperation` | — | automount component | ✔️ |
| S.17 | `PolkitAuthenticationAgent` | — | `polkitAgent` component | ✔️ |
| S.18 | `SecureTextBuffer` | **before** `KeyringPrompt` | keyring C creates these buffers | ✔️ |
| S.19 | `KeyringPrompt` | S.18 | `keyring` component | ✔️ |
| S.20 | `InvertLightnessEffect` | — | magnifier a11y — **low** | ✔️ |
| S.21 | `get_file_contents_utf8_sync` | **with** `PerfLog` | scripting / dump path | ✔️ |
| S.22 | `write_string_to_stream` | **with** `PerfLog` | `scripting.js` dump | ✔️ |
| S.23 | `PerfLog` | S.21–S.22 | looking-glass / scripts — **low** | ✔️ |
| S.24 | `TrayIcon` | **with** `TrayManager` | XEmbed child — **low** | ⏳ |
| S.25 | `TrayManager` | S.24 | legacy tray / Ubuntu appindicators — **low** | ⏳ |
| S.26 | `QrCodeGenerator` | — | distro GIR only; HEAD uses GnomeQR — **last / skip** | ⏳ |

**Done when:** S.1–S.26 ✔️ (or dated defer, especially S.23–S.26).

---

## Phase D — TEMP deny burn-down (Meta / Clutter / St)

**🔷** `⏳` Close every **TEMPORARY** row in the deny lists. Meta/Clutter/St
are **generated** (`gi-stub-gen` + deny + overrides + Helpers). Compile gaps
are already **0**; remaining debt is these TEMP rows.

**Files:** `src/gi-stub-gen/{Meta,Clutter,St}.deny` — keep TEMPORARY blocks
together. **PERMANENT** rows (hand overrides, ABI locks, signal/method
clashes) stay — do not undeny them into generated stubs.

**Per row:** undeny + wire, or move to PERMANENT with a one-line reason that
is not “later.”

**Prove:** rebuild client typelibs; nested or named smoke for the symbol;
exit check: `rg 'TEMPORARY' src/gi-stub-gen/*.deny` has no symbol rows left
(header only).

### D1 — Clutter TEMP

| # | Symbol | Notes | Status |
| - | ------ | ----- | ------ |
| D1.1 | `FrameClock.get_max_render_time_debug_info` | GString return mapping | ⏳ |
| D1.2 | `PaintNode.add_rectangles` noop | array wire | ⏳ |
| D1.3 | `PaintNode.add_texture_rectangles` noop | array wire | ⏳ |
| D1.4 | `ActorBox.from_vertices` noop | array wire | ⏳ |
| D1.5 | `Actor.get_abs_allocation_vertices` noop | array wire | ⏳ |
| D1.6 | `Actor.accessible` / `get_accessible` / `set_accessible` | Atk peers / client-local | ⏳ |

### D2 — St TEMP

| # | Symbol | Notes | Status |
| - | ------ | ----- | ------ |
| D2.1 | `Theme.get_custom_stylesheets` noop | `Gio.File` list | ⏳ |
| D2.2 | `ImageContent.set_data` noop | raw pixmap bytes | ⏳ |
| D2.3 | `IconTheme.get_icon_sizes` noop | `int[]` return | ⏳ |
| D2.4 | `FocusManager.navigate_from_event` noop | Event on wire | ⏳ |

### D3 — Meta TEMP

| # | Symbol | Notes | Status |
| - | ------ | ----- | ------ |
| D3.1 | `Window.set_compositor_private` noop | JS only gets; revisit if client must attach | ⏳ |
| D3.2 | `BackgroundImageCache.purge` noop | | ⏳ |
| D3.3 | `Background.set_blend` noop | | ⏳ |
| D3.4 | `ShapedTexture.set_mask_texture` noop | | ⏳ |
| D3.5 | `MultiTexture.new` / `new_simple` temporary | | ⏳ |
| D3.6 | `WaylandClient.new` / `spawnv` | hand override today; reclassify when [`0.8.4`](0.8.4-wayland-subprocess-peer.md) lands | ⏳ |

**Done when:** TEMPORARY symbol lists empty (or only reclassified PERMANENT).

---

## Phase H — Hardening + tidy (deferred plans)

**🔷** `⏳` Pull these after Phase 0. Each has its own plan — do the work
there, tick here when that plan is ✔️/✅.

**Prove:** whatever that child plan’s prove section says; nest A/B still green.

| # | Plan | Why | Status |
| - | ---- | --- | ------ |
| H1 | [`0.8.3-rpc-lid-hardening.md`](0.8.3-rpc-lid-hardening.md) | hide/lock `rpc_lid` from GJS | ⏳ |
| H2 | [`0.8.4-wayland-subprocess-peer.md`](0.8.4-wayland-subprocess-peer.md) | compositor owns spawn; shell `RpcSubprocess` peer — needs a real caller (DING / etc.) | ⏳ |
| H3 | [`0.7.10-src-directory-layout.md`](0.7.10-src-directory-layout.md) | `src/` rename / `Gsr` tidy — anytime after 0.8 | ⏳ |

**Done when:** H1–H3 closed or deferred with a dated reason.

---

## Phase U — Host-session candidate

**🔷** `⏳` Prove “restart shell, compositor stays” outside the Weston nest.

**Context:** product goal is a shell client you can restart without tearing
down the desktop. Nested Weston prove stays the default agent path.

**Prove:** explicit opt-in runbook only — **🚫** agents must not touch the
user’s live session without an ask in that message.

| # | Work | Status |
| - | ---- | ------ |
| U1 | Scripted host-candidate runbook (opt-in; not agent default) | ⏳ |
| U2 | Crash / restart `gnome-shell-rpc`; compositor + windows survive | ⏳ |
| U3 | Packaging notes: Depends matching `gnome-shell` JS; no shipped `js/` | ⏳ |

**Related (parallel, not blocked on U):** [`0.9-multi-shell-series.md`](0.9-multi-shell-series.md)
— per-release `libmutter-rpc-NN` packaging after 0.8 ✅.

**Done when:** U1–U3 done; at least one supervised host prove recorded.

---

## Plan done when

Phase **0**, **S**, **D**, **H**, **U** each meet their own **Done when**
(or dated defer). Then archive this plan under `done/`.

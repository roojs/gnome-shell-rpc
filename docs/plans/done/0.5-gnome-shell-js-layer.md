# 0.5 — Layer for GNOME Shell JavaScript (not a Vala replica)

**User goal:** nested **gnome-shell boots**. Everything else is a means — only do it if it unblocks boot.


Checklist: **`docs/guide-to-writing-plans.md`**.

Vala: **`docs/coding-standards.md`**.

**Status:** **PROPOSED**

**Parent:** [`0.1-decouple-gnome-shell-vala-rpc.md`](0.1-decouple-gnome-shell-vala-rpc.md)

**Depends on:** [`0.3-minimal-shell-top-bar.md`](0.3-minimal-shell-top-bar.md) ✅ (RPC + fake-shell proven). **[`0.4`](0.4-server-spawns-shell-client.md) skipped for now** — start the JS client by hand.

**Next (scaling):** [`0.5.1-runtime-helpers-and-positional-args.md`](0.5.1-runtime-helpers-and-positional-args.md) — Runtime return helpers; positional `(dir, GValue)` args instead of `*Params`. **Integration:** [`0.6-libmutter-rpc.md`](0.6-libmutter-rpc.md) — package the client as `libmutter-rpc-16` so GJS / GNOME Shell treat it as mutter.

**RPC library:** OLLMchat `libocrpc`. Client `notify::` apply: [`8.3.5`](file:///home/alan/gitlive/OLLMchat/docs/plans/8.3.5-client-apply-notify-proxies.md) (placeholder).

---

## Agent rule: do not change the user’s session

Same as **0.1** — nested tests only.

---

## Course correction

**🔷** The product shell is **GNOME Shell JavaScript** (`js/ui`, extensions). This repo does **not** reimplement panel, overview, or Activities in Vala/GTK.

**🔷** We provide a **layer** GJS can run on: mutter keeps compositing; **stock** libgjs; our **typelib + generated stub `.so`** sit first on `GI_TYPELIB_PATH`. GJS `ffi_call`s our symbols; those stubs RPC. No GJS patch.

**🚫** Do **not** patch GJS / `Function::invoke` / `LD_PRELOAD` of GI.

**ℹ️** [`0.3`](0.3-minimal-shell-top-bar.md) fake-shell stays a **throwaway probe**. Do not grow it into the shell.

**🚫** Do not start a Vala overview / hotkey UI as the 0.5 deliverable.

**ℹ️** Early 0.1 text said “Vala first client” and listed “keeping gnome-shell JS as the first client” as out of scope. **That was the divergence.** This plan replaces that target.

---

## Split (what we replace vs keep)

| Keep (holy grail) | Replace (this repo’s layer) |
|-------------------|-----------------------------|
| gnome-shell **JS** (`js/ui`, `js/misc`, extensions as GJS) | In-process `libgnome-shell.so` **as UI in `MetaPlugin.start()`** |
| Mutter: DRM, Wayland, window actors | In-process GI → libmutter (same address space as GJS) |
| Policy/UX written in JS | A Vala/C **class per Meta type** on the client that wraps RPC |

**Plugin process (already 0.1–0.3):** `gnome-shell-rpc` = libdefault-style effects + `SocketListen` + `Ui` handlers. Completes map/minimize locally.

**Client process (0.5+):** **Stock** libgjs (`gjs-embed`). `GI_TYPELIB_PATH` prepends **our** `Meta-16` (or a toy namespace first). Typelib `shared-library` is our generated stub `.so`. GJS still does `imports.gi.Meta`; C symbols RPC to the plugin. GLib/Gio stay the real typelibs (later on the path).

---

## Client vs server (do not blur)

**GJS does not run in the plugin.** Two processes:

| | **Client** (GJS process) | **Server** (mutter + plugin) |
|--|--------------------------|------------------------------|
| Runs | Stock libgjs / SpiderMonkey, `js/ui`, our typelib + stub `.so` | DRM/Wayland, real `Meta*`, stage, `SocketListen`, apply ops |
| Crash | Shell JS / GJS / SpiderMonkey die | Compositor / session die |
| Stay up if the other dies | Windows keep painting | Client reconnects or exits; windows stay |

**What out-of-process buys:** kill the shell client → windows stay. That is the holy-grail crash story. We are **not** moving GJS into the plugin.

**What still kills the server:** bugs in mutter/Meta, or in plugin code that touches `Meta*` while handling an RPC. Compositor process was always that crash domain.

**🔷** Do **not** “RPC the whole of GJS onto the server.” Only **allowlisted Meta/Shell-shaped calls** cross the socket. GLib/Gio/Gtk/Json stay **local GI** in the client (no RPC).

**💩** Blind plugin-side `g_function_info_invoke` for every client request **does** widen what a buggy shell call can crash in mutter. Prefer curated handlers (like 0.3 `Ui.Display`) or a hard allowlist + validation. Generic GI re-invoke on the server is a later, explicit risk — not the echo-smoke design.

**ℹ️** Echo smoke: dummy server only prints the call. No mutter. No Meta. Crash target is nowhere near the compositor.

---

## The St / Clutter problem (must decide before a full port)

Stock GJS is not only `window.minimize()`. Panel and overview are **`St` widgets on mutter’s Clutter stage**.

| Approach | Meaning | 0.5 |
|----------|---------|-----|
| **A — Meta/Shell via GI relay** | `gi-stub-gen` from installed typelibs; stub `.so` RPCs. JS chrome still assumes a local stage | Grow generator + Meta/Shell stubs. Does **not** boot full `js/ui/main.js`. |
| **B — Remote the stage** | Clones, `St`, textures over RPC/dmabuf | Later; overlaps old “overview fds” work. |
| **C — Change js/ui to Wayland chrome** | JS kept, `St`-on-stage abandoned | **Replicating layout** — **🚫** unless the user reopens it. |

**🔷** `⏳` 0.5 is **approach A**: GI-shaped Meta/Shell calls work out of process. Document what still requires a stage and defer that.

**💩** `⏳` Full `js/ui/main.js` boot in 0.5? **No** until A is real and the stage gap is explicit.

---

## Generate from typelibs (not from JS archaeology)

**🔷** The relay is driven by **installed typelibs** (`Meta-16`, later `Shell`, …) via `gi-stub-gen`. Walk the library with GIRepository; emit stubs. **🚫** Do not start from a gnome-shell `js/` inventory to decide which symbols exist.

**ℹ️** Point `gi-stub-gen` at the typelibs you want to relay (`Meta-16`, later `Shell`, …). Those are separate from `St` / `Clutter` typelibs — you are not generating those unless you ask for them.

**ℹ️** Gio/GLib/Json stay **real** typelibs on the client (not shadowed).

**🚫** Do not hand-write a JS-facing `Remote.Window` tree. Fake-shell `Remote` stays throwaway. **Generated Vala stubs from typelib** (one symbol → RPC) are the client layer.

---

## What we replace: the typelib’s **shared library**, not GJS

GJS stays stock. It loads `Meta-16.typelib`, `dlopen`s `shared-library`, `ffi_call`s `c:identifier`.

**Wrong:** patch GJS (`Function::invoke`) or `LD_PRELOAD` GI. Thicker, fights an ABI we do not own.

**Right:** our typelib **first** on `GI_TYPELIB_PATH`, `shared-library` = **generated stub `.so`**. Each exported symbol is a thin stub that RPCs (name + args). Same JS: `imports.gi.Meta.window.minimize()`.

```
today:    GJS --GI--> libmutter.so.0
target:   GJS --GI--> our stub .so  --libocrpc--> plugin --> real MetaWindow
          (typelib first on GI_TYPELIB_PATH)
```

**🔷** Schema = typelib (namespace / symbol allowlist as needed for generator maturity). **`GenClient`** emits GI stub Vala + typelib pointing at our `.so`. Server side is **not** a second code generator — see **Next step** (relay onto real leased objects). **No Python.**

**🚫** Hand-written Vala proxies for GJS. Throwaway Vala bar proxies live under {@code src/fake-shell/} ({@code GnomeShellRpc.FakeShell}).

**ℹ️** Gio/GLib/Json stay **real** typelibs (our path only shadows `Meta` / toy namespace).

**ℹ️** GJS still wraps GObjects from our `get_type`. Those types live in the stub `.so`; instance identity for mutter is a **lease id** on the stub instance (see **Stub identity** below), not a Vala `Remote.Window` API.

**Wire names (domain vs RPC internals):**

| Kind | Example |
|------|---------|
| libocrpc internals | `RPC-Daemon.hello`, `RPC-Live-Remote.ref` |
| Domain (hyphen object + method) | `Meta-Display.minimize_window`, `Meta-Window.minimize`, `GiRpcSmoke.ping` |

**🚫** Do not put `RPC-` on Meta / Ui / toy handlers. Object prefix is `Meta-Display` (not `Meta.Display.…` — dispatch splits on the first `.`).

---

## Stub identity: lease id on the client GObject

Client-side `Meta.Window` (etc.) is a **stub GObject** in our `.so`. The real `Meta*` lives only in the plugin. Relay needs the server’s handle on every method call.

### How stubs appear

| Path | Who creates the real object | How the client stub gets the id |
|------|----------------------------|----------------------------------|
| **Server → client** | Mutter / plugin (`window-created`, query returns, …) | Notification or RPC result carries lease id; client constructs (or reuses) a stub and **attaches the id** |
| **Client → server** | Client calls a constructor / factory that is allowed to RPC | Server creates (or rejects), replies with lease id; client stub keeps that id |

**ℹ️** Many Meta types **cannot** be created by the client (`Meta.Display`, `Meta.Window` from the compositor, …). Those only arrive via the server path. A few types *might* be client-constructible later (allowlisted constructors) — same id attachment once the server answers.

**🔷** Prefer **`g_object_set_data` / `get_data`** for the **lease id only** on the stub instance — not for title or other snapshot fields (those are normal private/instance state). Method stubs: read lease → include id in the relay RPC.

**ℹ️** 0.3 already exports real `Meta.Window` on the server (`Connection.export`) and sends `Notification.id` / `object_type`. Stub layer should reuse that lease vocabulary, not invent a second id space.

**⏳** Details TBD: quark/key name, int vs uint64, map id→stub for reuse when the same window is notified again, dispose/unref vs `RPC-Live-Remote`.

---

## libgjs (installed: 1.82.1)

**ℹ️** Distro: `libgjs-dev` / pkg-config **`gjs-1.0`**. Public include is `<gjs/gjs.h>` → `context.h`, `coverage.h`, `error-types.h`, `mem.h`, `profiler.h`. Requires.private: `mozjs-128`, `gobject-introspection-1.0`.

**ℹ️** **No VAPI** and **no GIR** for `GjsContext`. (`GjsPrivate-1.0.typelib` under `/usr/lib/.../gjs/girepository-1.0/` is not the embed API.) Vala needs a **small hand vapi** for `Gjs.Context` if we embed from this repo.

Public embed surface (what we can set up without patching):

| API | Role |
|-----|------|
| `gjs_context_new()` / `gjs_context_new_with_search_path()` | Construct; **JS module search path** (`imports.searchPath`) |
| GObject props `search-path`, `program-name`, `program-path`, `profiler-enabled`, `profiler-sigusr2` | Same setup gnome-shell uses |
| `eval` / `eval_file` / `eval_module` / `eval_module_file` / `register_module` | Run our `smoke.js` |
| `define_string_array` / `set_argv` | `ARGV` etc. |
| `get_native_context()` | `JSContext*` (SpiderMonkey) — **not** a GI intercept |
| coverage / profiler / GC / debugger console | Not the 0.5 hook |

**🚫** There is **no public “intercept import” / “intercept GI invoke”** on this API. `gjs_importer_class` / `gjs_repo_class` are exported symbols **without headers** — internal JSClasses, not a supported hook.

**ℹ️** Typelib search is **GIRepository** (`prepend_search_path` / `GI_TYPELIB_PATH`), not `GjsContext` search-path. Search-path is for **JS files**.

**🔷** `✔️` Embed `GjsContext` (`gjs-embed` + `vapi/gjs-1.0.vapi`). Stock libgjs — no fork.

---

## GIR / typelib first (the layer)

**ℹ️** JS modules: `Gjs.Context` search-path. **GI namespaces:** `GI_TYPELIB_PATH` / `g_irepository_prepend_search_path`. First `Meta-16.typelib` wins. Mutter GIR: `shared-library="libmutter-16.so.0"`.

**🔷** Our typelib first, `shared-library` = generated stub `.so`, every `c:identifier` exported. That *is* simpler than patching GJS: public GI, stock libgjs, `gjs-embed` prepends the path.

**ℹ️** Generator must also emit `*_get_type` (and enough GObject class) for types GJS holds.

---

## Steps (first smoke)

Do these in order. Stop when echo works. No mutter. **Stock** `gjs-embed`.

1. **🔷** `✔️` **Embed libgjs:** `gjs-embed` + `vapi/gjs-1.0.vapi`.

2. **🔷** `✔️` **Toy GIR + stub `.so`:** handwritten `GiRpcSmoke-1.0.gir`; `libgirpcsmoke.so` now built from **generated** Vala + `GiStub.Runtime` (not a hand `Ping.vala`).

3. **🔷** `✔️` **Dummy echo server:** `gi-rpc-echo` on `$XDG_RUNTIME_DIR/gi-rpc-smoke.sock` (or `GI_RPC_SMOKE_SOCKET`). Prints `got this call: ping msg=…`, replies `echo:…`.

4. **🔷** `✔️` **`smoke.js`:** `imports.gi.GiRpcSmoke.ping('hello')`.

5. **🔷** `✔️` **Run:** echo + `GI_RPC_SMOKE_TYPELIB_DIR=./build/src ./build/src/gjs-embed --debug src/gjs-embed/smoke.js` → server log + `echo:hello`.

6. **🔷** `✔️` **Generator (Vala, no Python):** `Generator` + CLI via **libgirepository**. Meson: schema typelib → emit → `libgirpcsmoke.so` → final typelib. **🚫** GIR XML parser. Emit today: namespace functions, utf8→utf8 only.

7. **🔷** `✔️` **Client emit grows:** object methods + enums/structs can be named from typelib; **GiRpcSmoke** still the only wired RPC (`Runtime.call_utf8`).

8. **💩** ~~Full Meta dump with `GLib.error("… not wired")` as a “review artifact”~~ — **wrong turn.** Emitting thousands of dead stubs that cannot call RPC is not a step in the flow; it does not prove Meta, and it confuses “generated” with “wired.” Keep Meta emit for when `Runtime` (or relay) can actually send the call. Until then, prefer **not emitting** the body, or only emit allowlisted methods that have a real `Runtime` path.

9. **🔷** `✔️` **JS smoke** + **hand Meta mini** (`src/meta-mini/`, `libmetamini`). **⏳** Nested run of `meta-smoke.js` (launch → fetch → minimize).

---

## Next smoke: GJS GI invoke → dummy RPC (echo)

**🔷** `✔️` Run **stock** `gjs-embed`. Script uses GI as usual. Our stub `.so` (typelib first) **emits RPC**. Dummy server echoes. No mutter.

The RPC **does not have to reach mutter or succeed**. A **dummy server** that listens and **echoes** (“got this call”, dump method/args) is enough.

That proves the layer: GJS still thinks it is calling GI; the backing `.so` is ours.

**🚫** Not a GJS rewrite of `rpc-client` (hand-calling `OLLMrpc` / `Ui`).
**🚫** Not nested mutter, not fake-shell, not real `minimize` on a window.
**🚫** Not a client class per type.
**🚫** Not “inventory JS then decide symbols.”

**Later:** same generator against the real plugin. Stage chrome (`St`/Clutter) is a separate problem when JS needs it — not this generator step.

---

## Next step: minimal JS test → fill the stack (vertical slice)

**🚫** Do **not** invent methods/properties/signals on stock GI classes (`Meta.*`, later `Shell.*`, …). Stubs implement **real** typelib symbols only. Out-of-process bootstrap with no stock equivalent (e.g. `Meta.get_display()` for `global.display`) must be labeled as such — never add conveniences like a fake `Meta.Display.launch`. App launch: `get_startup_notification().create_launcher()` + `Gio.AppInfo.launch`.

**🔷** Do **not** drive this slice from `gi-stub-gen` / full Meta emit. **Hand-write** a tiny client-side **Meta** (GIR + Vala `.so`) with only what `meta-smoke.js` needs. Get launch → fetch → minimize working on nested mutter. **Then** use that hand Meta as the pattern for what GenClient should emit.

**🔷** The smoke must hit **real mutter on the nested desktop** (fetch + put). Echo-only is not the target.

**Order:**

1. **🔷** `✔️` **JS smoke** — `src/gjs-embed/meta-smoke.js`: launch → fetch → minimize. Needs `Meta.get_display()` bootstrap (not stock Meta).
2. **🔷** `✔️` **Hand Meta client** — `src/meta-mini/`: GI Meta stubs; wire `Meta-Display.*` (e.g. `Meta-Display.minimize_window`) → existing Ui handler.
3. **🔷** `⏳` Nested run: compositor spawns {@code gjs-embed} + {@code meta-smoke.js} (lightweight, no pid watch); launch → fetch → minimize.
4. **🔷** `⏳` **Learn → generate** — once the hand Meta works end-to-end, encode into **`GenClient`**, then retire the hand copy.
5. Rename `Generator` → `GenClient` when convenient (bookkeeping).

**Candidate JS:** [`src/gjs-embed/meta-smoke.js`](../../src/gjs-embed/meta-smoke.js)

| Half | Example | Why |
|------|---------|-----|
| **Launch** | `get_startup_notification().create_launcher()` + `Gio.AppInfo.launch` | Real Meta + Gio; not a fake `Display.launch` |
| **Fetch** | hand `get_display` + focus/list + title | Lease id on stub |
| **Put** | `minimize()` | Visible on nested desktop |

**ℹ️** Nested mutter + this plugin only (0.1 session rules).

**Server path (still):** not `GenServer` emit — plugin / relay only for what the hand Meta calls.

**🚫** Do not generate thousands of Meta stubs to “catch up” to the smoke.
**🚫** Do not expand GenClient until the hand Meta path works.

---

## 0.5 deliverable (minimum)

- **🔷** `✔️` Toy stub `.so` + typelib first; `gjs-embed` `ping` → echo server.
- **🔷** `✔️` Client emit: **GIRepository** → Vala stubs → `libgirpcsmoke.so` (utf8→utf8 namespace functions that actually RPC).
- **💩** Full Meta `.vala` of `not wired` errors — not a deliverable; drop that pattern.
- **🔷** `✔️` Hand Meta mini (`src/meta-mini/`) for `meta-smoke.js`; wire **`Meta-Display.*`** (no `RPC-` on domain handlers).
- **🔷** `⏳` Nested-mutter run: launch → fetch → minimize; then GenClient from that pattern.
- **🔷** `⏳` Rename **`GenClient`** when convenient.
- **🚫** Port `js/ui/main.js`.
- **🚫** Grow `src/fake-shell/`.
- **🚫** Vala overview.
- **🚫** JS-source inventory as the source of truth for symbols.

---

## How GJS sits on the layer (sketch)

```
dummy echo server                    gjs-embed (stock libgjs)
  print / reply “got this call”        imports.gi.GiRpcSmoke
         ▲                             our typelib + stub .so
         └──────── libocrpc ───────────┘
```

---

## Relation to earlier 0.5 text (hotkeys / overview)

Old 0.1 “Phase 5” (Super, dmabuf overview, plugin `map` notifications) is **not deleted** — it is **not this file’s exit**. Those matter when GJS overview needs stage/thumbnails (**approach B**). Track as later plans once A works.

Plugin `map` / `destroy` **notifications** may still be needed so JS can run wm.js logic without owning actors — add when the Meta/Shell relay needs them, not from a JS inventory pass.

---

## Exit

- **✔️** Echo server logged `ping` from `gjs-embed` via **our** typelib/stub `.so`.
- **✔️** Same path builds from **generated** Vala (`gi-stub-gen emit`), not a hand `Ping.vala`.
- No GJS patch. No mutter in this smoke.

---

## First smoke (draft)

```bash
./build/src/gi-rpc-echo --debug

GI_RPC_SMOKE_TYPELIB_DIR=./build/src \
  ./build/src/gjs-embed --debug src/gjs-embed/smoke.js
# server: got this call: ping msg=hello
# client: echo:hello
```

Nested mutter comes **after** this.

---

## Generator (typelib via GIRepository)

**🔷** Read symbols from the **typelib** (`GI.Repository.require`), not GIR XML. Emit **client** Vala stubs; package with `g-ir-compiler --shared-library=…`.

**🚫** Do not build a full GIR XML parser / include merger.

**Layout (`src/gi-stub-gen/`):**

| | Status |
|--|--------|
| **`GenClient.vala`** | `⏳` next — client GI stubs (today's `Generator` logic moves here) |
| **`GenServer.vala`** | **🚫** not the plan — server side is relay design, not generated handler Vala |
| **`Application.vala`** | `✔️` CLI only — `emit NAMESPACE VERSION --out=…` |

**🔷** `✔️` Client emit by `InfoType` (function / object / enum / flags / constant / struct / interface / callback).
**🔷** `✔️` Callables: utf8→utf8 namespace functions → `Runtime.call_utf8`.
**💩** Emitting other callables as `GLib.error("… not wired")` (and the Meta “review” dump) — **do not continue**; either skip emit until wired, or emit a real `Runtime` call once the relay exists.
**🔷** `✔️` Meson builds `libgirpcsmoke.so` from generated Vala + hand `Runtime` / `PingParams` / `PingResult`.
**🔷** `⏳` Wire object methods / richer signatures to the **server relay** shape; constructors; array/`GList` types. Meta emit follows that, not error stubs.

```bash
./build/src/gi-stub-gen emit GiRpcSmoke 1.0 --typelib-dir=./build/src \
  --allow=ping --out=./build/src/GiRpcSmoke_generated.vala

./build/src/gi-rpc-echo --debug
GI_RPC_SMOKE_TYPELIB_DIR=./build/src \
  ./build/src/gjs-embed --debug src/gjs-embed/smoke.js
```

---

## Open questions

- **⏳** **Server apply:** RPC relay onto real leased GObjects (wire format, allowlist, libocrpc vs plugin) — better than generating `GenServer` handler classes.
- **⏳** Stub lease id: confirm `set_data` / quark; id→stub reuse; dispose vs live `ref`/`unref`.
- **⏳** Which Meta constructors (if any) are client-allowed vs server-only.
- **⏳** How `global` / `Shell.Global` is filled without in-process libgnome-shell.
- **⏳** When to tackle St/stage (approach B) — not 0.5.

# 0.5.3 — Partial Clutter relay (leased compositor actors)

**User goal:** nested **gnome-shell boots**. Everything else is a means — only do it if it unblocks boot.


Checklist: `docs/guide-to-writing-plans.md`.

**Status:** **IN PROGRESS** — POC + transform RPC **✔️** (compile). Nested `clutter-smoke.js` **✔️** (`Clutter-Actor.show`; visible false→true). **No** `Rpc.Helper`. Notify sync still **⏳** (`get_position` still `0,0` after `set_position`).

**Parent:** [`0.5-gnome-shell-js-layer.md`](0.5-gnome-shell-js-layer.md)

**Depends on:** [`0.5.2-close-gi-stub-gaps.md`](0.5.2-close-gi-stub-gaps.md) (Meta `call_values` emit, GObject list returns). **⏳** in progress.

**libocrpc:** [`8.4.8`](file:///home/alan/gitlive/OLLMchat/docs/plans/done/RPC-8.4.8-rpc-live-gtype-explicit-alias.md) `Bin.register_alias` **✔️**. This repo maps `MetaWindowActorX11` / `MetaWindowActorWayland` → `Meta-WindowActor` and `MetaCompositorServer` → `Meta-Compositor` in `Server.start()` **✔️**. Nested list return **✔️**.

**libocrpc:** [`Gi.dispatch`](file:///home/alan/gitlive/OLLMchat/libocrpc/Gi.vala) + `Gi.register` for **Meta** and **Clutter** **✔️**.

---

## Problem

Out-of-process client loads **real libclutter** and our **Meta stub `.so`**. Generated types such as `Meta.WindowActor` subclass `Clutter.Actor`.

GObject dispatch:

| Call | Lands in |
|------|----------|
| `windowActor.freeze()` | Meta stub → RPC ✔️ |
| `windowActor.show()` | **libclutter** on a hollow client actor unless leased override runs |

Extension authors cannot be told to avoid Clutter. **Compositor-backed** actors need a **lease proxy** that forwards selected Clutter APIs to the server. **Local `St` chrome** still uses real libclutter on a client stage (later).

**ℹ️** GIR often types list returns as `Clutter.Actor` when elements are really `Meta.WindowActor` (`Compositor.get_window_actors`). Vala metadata already fixes this; generator uses `Meta.overrides` `list_elem=WindowActor`.

---

## Goal

**🔷** **Partial Clutter** — not shadowing all of `Clutter-16`. Only forward Clutter methods on **leased** instances of types Meta exposes (`WindowActor`, returned `Clutter.Actor` leases, …).

**🔷** **POC:** one round-trip — `compositor.get_window_actors()[0].show()` hits server `Clutter.Actor.show` on the real window actor.

**🚫** Full paint pipeline, stage hosting, `St` on client — see approach B in 0.5 (remote stage). This plan is **lease forwarding**, not renderer relay.

**🚫** `Rpc.Helper.WindowActor` for `show` / `hide` / `get_visible` — stock `Clutter-Actor.*` + `Gi.dispatch`.

---

## Architecture

```text
GJS  →  Meta.WindowActor.show()
         └─ override in stub .so (leased?)
              ├─ yes → Runtime.call_values("Clutter-Actor.show", this)
              └─ no  → base Clutter.Actor.show()  (local St widgets)
Server Gi.dispatch → real window actor (is Clutter.Actor)
```

**Wire names:** use stock GI aliases — `Clutter-Actor.show`, not invented Meta symbols.

**Lease id:** client stub carries `gsr-lease-id` (from `Runtime.attach_lease` after decode). Server resolves `Request.lease_id` → live pointer.

---

## Method categories (grow over time)

| Category | Examples | Client (leased) |
|----------|----------|-----------------|
| **Geometry / visibility** | `show`, `hide`, `get_visible` | **✔️** override; nested ✔️ |
| **Transform** | `set_position`, `set_size`, `get_position` | **✔️** RPC; nested ✔️ (`set_*` on the wire); notify sync is phase 2 (`pos=0,0`) |
| **Scene graph** | `add_child`, `remove_child`, `get_parent` | **⏳** RPC |
| **Paint / GL** | `queue_redraw`, `paint` | **⏳** deny or Helper |
| **Paint / GL** | `paint_to_content` (Meta) | **✔️** Helper + memfd ([0.5.5](0.5.5-remaining-meta-stub-gaps.md) **D**); nested smoke **⏳** |
| **Signals** | `connect('notify::visible')` | **⏳** `RPC-Live-Subscribe` |

**⏳** Audit gnome-shell `js/` + common extensions for Clutter calls on `WindowActor` / returned actors → minimum RPC surface.

---

## POC scope (this repo)

### Server

- **🔷** `✔️` `Gi.register("Clutter", "16")` beside Meta.
- **🔷** `✔️` `RPC-Bootstrap.get_display` — export real `Meta.Display` lease.
- **🔷** `✔️` `Bin.register_alias` for concrete actor / compositor GTypes after `Gi.register`.

### Generator

- **🔷** `✔️` `src/gi-stub-gen/Meta.overrides` — `Compositor.get_window_actors list_elem=WindowActor`.
- **🔷** `✔️` Generated `get_window_actors` is `GLib.List<WindowActor>` (off the gap list).
- **🔷** `✔️` `WindowActor.override.vala` spliced (`show` / `hide` / `get_visible` / `set_position` / `set_size` / `get_position`). Not a Meta deny — these are Clutter methods.

### Client (`meta-mini` spike)

- **🚫** `ClutterLease` class — not used. Bodies live on `WindowActor`.
- **🔷** `✔️` `Meta.WindowActor` — visibility + transform trio (same bodies as the override file). Mini GIR lists `set_position` / `set_size` / `get_position` so GJS does not call stock Clutter (those are not virtual).
- **🔷** `✔️` `Meta.Compositor.get_window_actors()` — `call_values` + list unpack.
- **🔷** `✔️` `Meta.Display.get_compositor()`.
- **🔷** `✔️` `Runtime.attach_lease()` — copy `Client.proxies` handle → `gsr-lease-id` on decoded GObjects (`do_call` result).

### Smoke

- **🔷** `✔️` `src/gjs-embed/clutter-smoke.js` exists (`show` + `set_position` / `set_size` / `get_position`).
- **🔷** `✔️` Nested run (`GI_META_SMOKE=clutter-smoke`).

```bash
GI_META_SMOKE=clutter-smoke dbus-run-session ./build/src/gnome-shell-rpc --debug --wayland --nested
```

---

## Phases (after POC)

| Phase | Work |
|-------|------|
| **POC** | **✔️** compile + nested `clutter-smoke.js` |
| **1** | Generator: emit more Clutter overrides from audit list |
| **2** | **⏳** Property notify sync (visible, x, y, width, height) |
| **3** | Deny / document paint-only APIs on leased types |
| **4** | Stage / `St` — explicit fork (local chrome vs remote stage) |

---

## Open questions

1. **✔️** Do **not** shadow a Clutter typelib. Overrides live on Meta subclasses (`WindowActor`). Real Clutter stays on the client.
2. **✔️** `Runtime.attach_lease` on every `do_call` result object.
3. **⏳** Fail build if leased type missing override for a Clutter vfunc? (**💩** warn-only until audit lands.)

---

## Exit (POC done)

- **🔷** `✔️` Server log shows `Clutter-Actor.show` on a real window actor.
- **✔️** `Meta_window_generated.vala` wires `get_window_actors` as `GLib.List<WindowActor>`.
- **🔷** `✔️` `clutter-smoke.js` completed (`after show(): visible=true`).
- **🔷** `⏳` Plan lists audit-driven method backlog for phase 1.

**ℹ️** This POC does **not** by itself move the Meta-16 gap scoreboard — `show` / `hide` / `get_visible` are Clutter methods. Priority **D** Meta symbols (grab / pad / `paint_to_content`) are closed off the gap list via deny + Helper in [0.5.5](0.5.5-remaining-meta-stub-gaps.md) (**✔️** wire; nested smoke **⏳**).

---

## Agent rule

Same as **0.5** — nested tests only; no host session mutation.

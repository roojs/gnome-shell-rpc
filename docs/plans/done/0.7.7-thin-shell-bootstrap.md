# 0.7.7 — Thin shell bootstrap (no libshell for C boot)

**User goal:** nested `**mutter-rpc` + `gnome-shell-rpc`** boots (Wayland window). Everything else is a means — only do it if it unblocks that.

**Status (archive):** **✔️** boot corridor landed 2026-09-11 — follow-on [`0.8-init-complete-and-interaction.md`](../0.8-init-complete-and-interaction.md).

Checklist: `docs/guide-to-writing-plans.md`.

**Corridor loop (mandatory):**

1. **Live** — run `mutter-rpc --wayland --nested` (**~5s**); note the abort / wall.
2. **Mock** — reproduce that same failure under `gi-rpc-smoke.sh`. HelperMock / GiMock arms exist to **replay the live call path quickly** so we reach the crash site without a compositor — not to invent APIs or scout past the live wall.
3. **Fix on mock** — iterate until mock clears that wall.
4. **Back to live** — re-run nested Wayland (**~5s**); only then advance the live cursor.

**Live timeout:** **5s**. Abort / wall shows up well under that. If nested is still alive past ~5s without `terminate_with_error`, treat it as **probably booting** (success enough to stop watching for that run).

Mock does **not** count as product progress. Mock + helpers = **fast replay of the live crash**.

**Status:** **ARCHIVED** — Phase C **S0** ✔️ · **S1** ✔️ · **S2 / T-030** boot path ✔️ (init-complete + input → **0.8**).

**Where we are (2026-09-08 evening, Wayland nested):**  
Hard walls **L1–L7** ✔️. Soft util/Global/App icons ✔️. ThemeContext `rpc_lid` ✔️. UTF8 null + non-null (dup) ✔️. Actor `get_accessible` ✔️. `Shell.CameraMonitor` minimal ✔️. `call_sync` ✔️. `Helper-BackgroundImageCache.load` ✔️ (no `-32602`).

**What’s left for T-030 / S2:** soft stall after wallpaper `load` (Idle / chrome); ding / `startup-complete`; **5e** / Meta binds when hit.

**Corridor:** live (~5s) discovers → mock replays that wall → fix → live re-prove. Mock ✔️ ≠ product ✔️.

**Parent:** `[0.7-gnome-shell-rpc-client.md](0.7-gnome-shell-rpc-client.md)`

**Supersedes:** vendored `**libshell-16.so`** + `**shell-global-rpc-append.c**` (`[0.7.3](0.7.3-init-js-boot-nested.md)` row 3).

**Depends on:** `[0.6](0.6-libmutter-rpc.md)` Meta/Clutter stubs **✔️**; `[0.7.6](0.7.6-st-server-relay.md)` St RPC **✔️**; `[0.7.8](done/0.7.8-gi-rpc-noop-server.md)` mock smokes **✔️**

**🚫** Nested tests only — no `meson install` over the live session. **🚫** Fork vendor `js/`. **🚫** Bootstrap noops / NULL placeholders. **🚫** Edit OLLMchat from this tree (file bugs there).

---

## Init slice (sign **✅** before coding)


| Slice                | Bar                                                                     |
| -------------------- | ----------------------------------------------------------------------- |
| **S0**               | `shell-global-smoke.js` on thin host (no `libshell`)                    |
| **S1** (recommended) | stock `init.js` → loop runs → `main.start()` enters; walls → new T-rows |
| **S2**               | S1 + `Layout.LayoutManager` / `stage.add_child(uiGroup)`                |


---

## Nested backlog

**Do tags:** **OWN** write here · **LEASE** hold server object via stub `rpc_lid` · **RPC** wire method · **JS** upstream only · **SKIP** not this slice · **DONE** already landed

**Files (new/keep):** `src/shell-client/ShellApplication.vala` · `src/shell-gi/Global.vala` · `src/meson.build` (`vala_gir` → typelib) · `src/shell-js/` (gresource)

**Delete (libshell glue):** `src/shell-client/bootstrap.c`, `bootstrap.h`, `shell-global-rpc-append.c`, `shell-global-rpc.h`

```
gnome-shell-rpc thin host                          [S0]
├── meson: unlink libshell-16 + libst-16           OWN  src/meson.build, client-libs
├── host start: Runtime.register + display lease   OWN  ShellApplication.vala
├── GJS context + eval                             OWN  ShellApplication.vala
├── GI_TYPELIB_PATH=build/src first + mutter + PKGLIBDIR (Gvc)  OWN  rpc/Server.vala spawn
├── St.Viewport : Scrollable (hadjustment/vadjustment)         OWN  overrides-st + generator
└── Shell-16 (owned)                               OWN  src/shell-gi/
    ├── typelib + libshell-gi-16.so (vala_gir)     OWN  meson + Global.vala
    ├── ActionMode enum (main.js import)           OWN  ActionMode.vala         [S1] ✔️
    ├── Shell.App (env promisify only)             OWN  App.vala                 [S1] ✔️
    ├── namespace utils
    │   ├── util_touch_file_{async,finish}         SKIP until hit
    │   └── util_spawn_*                           OWN  Util.vala                [S1] ✔️
    └── Shell.Global                               OWN  Global.vala
        ├── get() singleton                        OWN  [S0] ✔️
        ├── signals: notify-error, shutdown        OWN  [S1] ✔️
        ├── signal: locate-pointer                 OWN  [S2] ✔️
        │
        ├── LEASE from compositor (fill at construct)
        │   ├── display / get_display()            LEASE Meta.Display           [S0] ✔️
        │   ├── stage / get_stage()                LEASE Clutter.Stage          [S0] ✔️
        │   ├── backend / get_backend()            LEASE Meta.Backend           [S0] ✔️
        │   ├── context / get_context()            LEASE Meta.Context           [S0] ✔️
        │   ├── compositor / get_compositor()      LEASE Meta.Compositor        [S0] ✔️
        │   ├── workspace_manager                  LEASE Meta.WorkspaceManager  [S2] ✔️
        │   ├── window_group                       LEASE Clutter.Actor          [S2] ✔️
        │   └── top_window_group                   LEASE Clutter.Actor          [S2] ✔️
        │
        ├── context.run_main_loop()                DONE  gi-stub/overrides/Context.override.vala
        ├── context.terminate_with_error()         DONE  same (+ Helper-Context server)
        │
        ├── begin_work() / end_work()              OWN  client refcount         [S1] ✔️
        ├── screen_width / screen_height           OWN  from stage size         [S2] ✔️
        ├── focus_manager                          LEASE St.FocusManager.get_for_stage  [S1] ✔️
        ├── settings                               OWN  GLib.Settings("org.gnome.shell") [S1] ✔️
        ├── datadir / userdatadir / session_mode   OWN  env/config              [S1] ✔️
        ├── get_current_time()                     OWN                          [S1] ✔️
        ├── notify_error()                         OWN  emit notify-error       [S1] ✔️
        │
        ├── compositor.disable_unredirect()        LEASE→ Meta.Compositor RPC   [S1] ✔️
        ├── compositor.enable_unredirect()         LEASE→ Meta.Compositor RPC   [S1] ✔️
        │
        ├── reexec_self / run_at_leisure / set_stage_input_region / get_pointer  SKIP
        ├── create_app_launch_context / get_window_actors                        SKIP
        ├── get_app_system / get_app_usage / get_window_tracker  SKIP / ✔️ tracker
        ├── get_window_manager → Shell.WM                      OWN  phase **6** ✔️
        ├── switcheroo-control / automation-script / debug-flags / force-animations / frame-* / state  SKIP
        ├── Shell.GLSLEffect … SquareBin …          OWN  load-time A             [T-032…]
        ├── util_systemd_* / Screenshot / WindowPreview / WorkspaceBackground
        │                                          OWN  load-time A             [T-034–039]
        ├── WindowTracker / AppUsage               OWN  call-time B             [T-040–041] ✔️
        └── St (already)
            ├── St.Widget lease-on-construct       DONE  0.7.6
            ├── St.describe_actor                  DONE  St_generated
            ├── St.FocusManager.get_for_stage      DONE  overrides-st (static)
            ├── St.Settings.get()                  DONE  Settings.override.vala
            ├── St.Theme (loadTheme client-owned)  DONE  overrides-st/Theme.override.vala
            └── St.ThemeContext get/set_theme      DONE  overrides-st/ThemeContext.override.vala

init.js / environment.js                           [S1] ✔️
├── import dependencies + environment              JS
├── globalThis.global = Shell.Global.get()         needs Global.get
├── Clutter/GObject/Cairo monkeypatches            JS
├── GLib.spawn_* → Shell.util_spawn_*              Util.vala
└── idle → import('./main.js') then main.start()   **loads ~137 modules**

Boot surface (static crawl 2026-09-02) — **think ahead, do not whack-a-mole**

**Why walls kept appearing:** `import('./main.js')` evaluates main’s **entire static
import graph** (~142 modules) before `start()` runs. Research that only lists
**Shell.*** symbols still misses **spawn env** and **non-Shell GI** typelibs.

**Crawl scope (mandatory before more nested runs):**
1. Shell extends + `Gio._promisify(Shell…)` at module top-level → table A
2. All `gi://Namespace` in that graph → typelib location vs spawn `GI_TYPELIB_PATH`
3. Call-time Shell in `_initializeUI` / early `start` → B; rest → C

**E — SPAWN ENV** (client `Rpc.Server` tip; 0.7.3 row 6 already required this)

| Need | Where | Status |
| --- | --- | --- |
| Our Meta/Clutter/St/Shell typelibs | `build/src` **first** | ✔️ |
| Mutter Mtk/Cogl extras | `MUTTER_TYPELIB_DIR` | ✔️ |
| **Gvc** (+ `libgvc.so`) | `GNOME_SHELL_PKGLIBDIR` (`/usr/lib/gnome-shell`) | ✔️ T-042 |
| System GI (Gio, NM, …) | default girepository | OK if path prepends, not replaces |

Static GI in load graph with typelib **only** under pkglibdir: **`Gvc`** (`ui/status/volume.js`).
`Malcontent` imported but not installed here — optional / later.

**A — LOAD-TIME Shell** (extends / `Gio._promisify` / must exist or module eval throws)

| Symbol | How hit | Status |
| --- | --- | --- |
| `ActionMode` | many | ✔️ |
| `App` | environment promisify | ✔️ |
| `Global` | environment | ✔️ |
| `GLSLEffect` | lightbox/messageList/screenshot extends | ✔️ T-032 |
| `SquareBin` | iconGrid extends | ✔️ T-033 |
| `util_spawn_*` | environment / ibus | ✔️ |
| `util_systemd_unit_exists` (+ finish) | ibusManager promisify-ns | ✔️ T-034 (smoke past) |
| `util_start_systemd_unit` (+ finish) | windowManager promisify-ns | ✔️ T-035 |
| `util_stop_systemd_unit` (+ finish) | windowManager promisify-ns | ✔️ T-036 |
| `Screenshot` (+ async methods for promisify) | screenshot.js promisify-proto | ✔️ T-037 |
| `WindowPreview` | windowPreview.js extends | ✔️ T-038 |
| `WorkspaceBackground` | workspace.js extends | ✔️ T-039 |

**B — CALL-TIME in `_initializeUI` / early start** (after A+E; before or with Layout)

| Symbol | Notes | Slice |
| --- | --- | --- |
| `WindowTracker.get_default()` | main.js `_initializeUI` | ✔️ T-040 |
| `AppUsage.get_default()` | same | ✔️ T-041 |
| `Layout.LayoutManager` | corridor — T-030 scoreboard (past hard walls; soft gaps) | **S2** ⏳ T-030 |
| `util_sd_notify` | hit at main L320 — **ported** (`Util.vala`); live `notify_ready` | soft → OWN ✔️ |
| `util_has_x11_display_extension` | X11 path | SKIP nested Wayland unless hit |

**C — CALL-TIME elsewhere in the eager graph** (own when hit or batch after B)

Classes: `AppSystem`, `Stack`, `BlurEffect`, `InvertLightnessEffect`, `MountOperation`,
`CameraMonitor`, `WindowPreviewLayout` · Enums: `AppState`, `AppLaunchGpu` ·
Funcs: `util_touch_file_{async,finish}` **✔️**, `util_set_hidden_from_pick`,
`util_translate_time_string`, `util_get_*` (**`util_get_uid` ✔️**), `util_regex_escape`,
`util_create_pixbuf_from_data`, `util_check_cloexec_fds`, `time_change_source_new` **✔️**

Also hit+ported on Global: `create_app_launch_context`, `get_persistent_state` /
`set_persistent_state`. App icons: `icon` / `create_icon_texture` **✔️** (dash).

**Not GI** (D-Bus names like `Shell.Notifications` / `Shell.Extensions`) — ignore for typelib.

main.start() / _initializeUI
├── **A** load-time table above                         do as one batch
├── WindowTracker / AppUsage                           B ✔️
├── reloadTheme / loadTheme / _loadDefaultStylesheet  B ✔️ (St.Theme + ThemeContext overrides)
├── _loadWorkspacesAdjustment (St.Adjustment)        ✔️ phase 1 — T-043
├── AnimationsSettings                               ✔️ phase 2 mocks (Backend + St-Settings)
├── Layout.LayoutManager._init                       ⏳ T-030 — hard walls L1–L7 ✔️; soft gaps remain
└── gresource themes/icons                             later

was libshell attach (do NOT port as noops)
├── st_entry_set_cursor_func / st_clipboard_set_selection / stage repaint+perf  SKIP S1
├── ui-scaling-factor / update_scaling_factor        OWN/RPC Meta settings       [S2] ✔️
    │   ├── Meta.Backend.get_settings                gi-stub/overrides/Backend.override.vala + c-meta-shell-gaps.c
    │   └── Meta.Settings.get_ui_scaling_factor      gi-stub/overrides/Settings.override.vala
└── shell_wm_new                                   SKIP
```

---

## TODO table (tick in Phase C)


| ID    | Slice | Symbol                                                                                     | Do        | File(s)                                                                                 | Status         |
| ----- | ----- | ------------------------------------------------------------------------------------------ | --------- | --------------------------------------------------------------------------------------- | -------------- |
| T-001 | S0    | Unlink `libshell-16` / client `libst-16` from default `gnome-shell-rpc`                    | OWN       | `src/meson.build`, `gnome-shell/client-libs/meson.build`                                | ✔️             |
| T-002 | S0    | Host start: RPC + `Runtime.register()` + fill `Shell.Global` leases — **no** libshell init | OWN       | `ShellApplication.vala`, `Global.vala`                                                  | ✔️             |
| T-003 | S0    | Own GJS context + eval script/gresource                                                    | OWN       | `ShellApplication.vala`                                                                 | ✔️             |
| T-004 | S0    | `Shell-16.typelib` + `libshell-gi-16.so` via `**vala_gir**` from Vala (not hand GIR)       | OWN       | `src/shell-gi/`, `src/meson.build`                                                      | ✔️             |
| T-005 | S0    | `Shell.Global.get()` singleton                                                             | OWN       | `src/shell-gi/Global.vala`                                                              | ✔️             |
| T-006 | S0    | `Global.display` / `get_display()`                                                         | LEASE     | `Global.vala`                                                                           | ✔️             |
| T-007 | S0    | `Global.stage` / `get_stage()`                                                             | LEASE     | `Global.vala`                                                                           | ✔️             |
| T-008 | S0    | `Global.backend` / `get_backend()`                                                         | LEASE     | `Global.vala`                                                                           | ✔️             |
| T-009 | S0    | `Global.context` / `get_context()`                                                         | LEASE     | `Global.vala`                                                                           | ✔️             |
| T-010 | S0    | `Global.compositor` / `get_compositor()`                                                   | LEASE     | `Global.vala`                                                                           | ✔️             |
| T-011 | S0    | Smoke: `shell-global-smoke.js` without libshell                                            | —         | `src/gjs-embed/shell-global-smoke.js`                                                   | ✔️             |
| T-012 | S1    | `Meta.Context.run_main_loop` / `terminate*`                                                | DONE      | `gi-stub/overrides/Context.override.vala`                                               | ✔️             |
| T-013 | S1    | `Global.begin_work` / `end_work`                                                           | OWN       | `Global.vala`                                                                           | ✔️             |
| T-014 | S1    | `Meta.Compositor.disable_unredirect` / `enable_unredirect`                                 | RPC       | `meta-mini/Compositor.vala` (Gi)                                                        | ✔️             |
| T-015 | S1    | `Global` signals `notify-error`, `shutdown`                                                | OWN       | `Global.vala`                                                                           | ✔️             |
| T-016 | S1    | `Global.notify_error()`                                                                    | OWN       | `Global.vala` (signal emit)                                                             | ✔️             |
| T-017 | S1    | `Global.settings` → `GLib.Settings("org.gnome.shell")`                                     | OWN       | `Global.vala`                                                                           | ✔️             |
| T-018 | S1    | `Global.datadir` / `userdatadir` / `session_mode`                                          | OWN       | `Global.vala`                                                                           | ✔️             |
| T-019 | S1    | `Global.focus_manager` ← `St.FocusManager.get_for_stage`                                   | LEASE     | `Global.vala`                                                                           | ✔️             |
| T-020 | S1    | `Global.get_current_time()`                                                                | OWN       | `Global.vala`                                                                           | ✔️             |
| T-021 | S1    | `Shell.ActionMode` enum                                                                    | OWN       | `src/shell-gi/ActionMode.vala`                                                          | ✔️             |
| T-022 | S1    | Verify `St.Settings.get` / `St.describe_actor`                                             | DONE/RPC  | `Settings.override.vala` + `St_generated`                                               | ✔️             |
| T-023 | S1    | `Shell.util_spawn_*` (touch_file SKIP until hit)                                           | OWN       | `src/shell-gi/Util.vala`                                                                | ✔️             |
| T-024 | S1    | gresource `ui/init.js` + `misc/config.js` without libshell                                 | OWN       | `src/shell-js/`                                                                         | ✔️             |
| T-025 | S1    | Eval stock `init.js`; first `main.js` wall → new T-row                                     | —         | nested log                                                                              | ✔️ → **T-032** |
| T-031 | S1    | Minimal `Shell.App` for `environment.js` promisify                                         | OWN       | `src/shell-gi/App.vala`                                                                 | ✔️             |
| T-032 | A     | `Shell.GLSLEffect`                                                                         | OWN       | `shell-gi/` + Helper                                                                    | ✔️             |
| T-033 | A     | `Shell.SquareBin`                                                                          | OWN       | `shell-gi/SquareBin.vala`                                                               | ✔️             |
| T-034 | A     | `util_systemd_unit_exists` (+ finish)                                                      | OWN       | `Util.vala`                                                                             | ✔️             |
| T-035 | A     | `util_start_systemd_unit` (+ finish)                                                       | OWN       | `Util.vala`                                                                             | ✔️             |
| T-036 | A     | `util_stop_systemd_unit` (+ finish)                                                        | OWN       | `Util.vala`                                                                             | ✔️             |
| T-037 | A     | `Shell.Screenshot` (promisify surface)                                                     | OWN       | `shell-gi/`                                                                             | ✔️             |
| T-038 | A     | `Shell.WindowPreview`                                                                      | OWN       | `shell-gi/`                                                                             | ✔️             |
| T-039 | A     | `Shell.WorkspaceBackground`                                                                | OWN       | `shell-gi/`                                                                             | ✔️             |
| T-042 | E     | Spawn tip: `GNOME_SHELL_PKGLIBDIR` for Gvc (0.7.3 row 6 regression)                        | OWN       | `rpc/Server.vala`                                                                       | ✔️             |
| T-040 | B     | `Shell.WindowTracker.get_default`                                                          | OWN       | `shell-gi/WindowTracker.vala`                                                           | ✔️             |
| T-041 | B     | `Shell.AppUsage.get_default`                                                               | OWN       | `shell-gi/AppUsage.vala`                                                                | ✔️             |
| T-026 | S2    | `Global.workspace_manager`                                                                 | LEASE     | `Global.vala`                                                                           | ✔️             |
| T-027 | S2    | `Global.window_group` / `top_window_group`                                                 | LEASE     | `Global.vala`                                                                           | ✔️             |
| T-028 | S2    | `Global.screen_width` / `screen_height`                                                    | OWN       | `Global.vala`                                                                           | ✔️             |
| T-029 | S2    | UI scaling via Meta settings                                                               | RPC/OWN   | `Global.vala`, `Backend.override.vala`, `Settings.override.vala`, `c-meta-shell-gaps.c` | ✔️             |
| T-030 | S2    | `Layout.LayoutManager` path green                                                          | JS+leases | `layout.js`, `HelperMock`/`MockBootGraph`, St/Clutter client stubs                      | ⏳              |
| T-043 | S2    | `St.Adjustment` client-local props (`new St.Adjustment({…})`)                              | OWN       | `overrides-st/Adjustment.override.vala` + `St.deny`                                     | ✔️             |
| T-044 | S2    | gi-stub-gen nullable GObject returns (omitted RPC `retval`)                                | OWN       | `gi-stub-gen/Generator.vala`                                                            | ✔️             |


**Delete with T-001/T-002:** `bootstrap.c`, `bootstrap.h`, `shell-global-rpc-append.c`, `shell-global-rpc.h`. **Bucket C** (call-time in eager graph): batch or hit-driven after A+B — see tree. **SKIP:** full Shell generator, client `libst-16`, X11-only helpers unless nested hits them.

**Phase C rule:** implement **A+E** before more nested runs; then **B**; **S2** leases for Layout.
Do not nest-debug the next Shell symbol until spawn GI crawl (table E) is green.

---

## T-030 — scoreboard (`main.js` `_initializeUI`)

**Bar (S2):** stock `_initializeUI` on **Wayland nested** through chrome + `layoutManager.init()` / `overview.init()` without hard abort; ~5s alive counts as “probably booting.”

**Span:** `_initializeUI` = `**main.js` L196–384**.

### How to read marks


| Mark | Meaning                                                     |
| ---- | ----------------------------------------------------------- |
| ✔️   | Path ran far enough to continue (construct / RPC OK)        |
| soft | Threw or logged CRITICAL but `**_initializeUI` kept going** |
| ⏳    | Not done / not observed yet / hard wall if hit              |
| —    | N/A on that column                                          |


**Corridor:** Wayland discovers → mock replays → fix → Wayland re-prove (~5s). Advance **Wayland** only on live evidence. Mock scout ≠ product.

### Progress through `_initializeUI`


| `main.js` | What                                                                              | Wayland | Notes                                           |
| --------- | --------------------------------------------------------------------------------- | ------- | ----------------------------------------------- |
| L205–214  | Tracker / AppUsage / theme / icons / stylesheet / workspaces / AnimationsSettings | ✔️      |                                                 |
| L217–222  | `LayoutManager` + `uiGroup`                                                       | ✔️      | L1–L2                                           |
| L224–228  | PadOsd → Xdnd / CtrlAltTab / OSD                                                  | ✔️      | L3 seat                                         |
| L229–236  | Overview / KbdA11y / WM / Magnifier / LocatePointer / ScreenShield                | ✔️      | constructs; not full init yet                   |
| L238–250  | InputMethod / ScreenshotUI / MessageTray / **Panel** / Keyboard / Components      | ✔️      | **L5–L7** cleared Switch ease                   |
| L252–273  | Introspect / BreakManager / TimeLimits + shutdown hook                            | ✔️      |                                                 |
| L275–276  | `**layoutManager.init()` / `overview.init()**`                                    | ✔️      | reached (passed before L320)                    |
| L278–312  | PointerA11y + display/context signal hooks                                        | ✔️      |                                                 |
| L316–323  | EndSessionDialog + idle `util_sd_notify` / `notify_ready`                         | ✔️      | Soft util ported; live `notify_ready`           |
| L327–329  | ExtensionDownloader / **ExtensionManager.init**                                   | soft→✔️ | ThemeContext `rpc_lid` theme map — re-prove ding |
| L339–345  | optional automationScript                                                         | —       | not used                                        |
| L347–383  | `startup-complete` handler                                                        | ⏳       | not confirmed fired                             |
| L384      | `_initializeUI` returns                                                           | soft    | process stays up; chrome still incomplete       |


**Prove log (L7):** `/tmp/mutter-rpc-l7-stock.log`. **Live soft-util prove:** `~/.cache/gnome-shell-rpc/org.gnome.ShellRpc.debug.log` (2026-09-08 ~18:52+).

### Soft gaps hit while `_initializeUI` continues

These are **not** the old L7 hard wall:


| Symptom                                                                     | Likely locus                 | Status                   |
| --------------------------------------------------------------------------- | ---------------------------- | ------------------------ |
| `Shell.util_sd_notify` / Global persistent / launch / touch / time_change / uid | `Util.vala` / `Global.vala` / `TimeChangeSource.vala` | **✔️** live |
| `app.get_icon` / `create_icon_texture`                                      | `App.vala`                   | **✔️** ported |
| Extension `theme is null`                                                   | `ThemeContext.override.vala` — `Gee.HashMap` by `rpc_lid` | **✔️** [done](../bugs/done/2026-09-08-themecontext-theme-null-rpc-lid.md) — re-prove |
| `malformed UTF-8` on `St-Icon.get_icon_name`                                | generator dup before Response free | **✔️** [non-null](../bugs/done/2026-09-08-st-icon-get-icon-name-utf8-nonnull.md) / [null done](../bugs/done/2026-09-08-st-icon-get-icon-name-utf8.md) |
| `this._output is undefined` (volume)                                        | `do_call` → `Client.call_sync` | **✔️** [reentrancy](../bugs/done/2026-09-08-volume-output-undefined-reentrancy.md) |
| `Meta-BackgroundImageCache.load` empty `-32602`                             | Helper URI + wait; client `is_loaded` | **✔️** [done](../bugs/done/2026-09-08-background-image-cache-load-file-wire.md) — soft quiet after load |
| `get_accessible() is null`                                                  | Actor local a11y             | **✔️** — [done](../bugs/done/2026-09-08-actor-get-accessible-null.md) |
| `CameraMonitor is not a constructor`                                        | Soft Shell port              | **✔️** — [done](../bugs/done/2026-09-08-shell-camera-monitor-missing.md) |
| `get_cogl_context`                                                          | Screenshot shoot (**5e**)    | when ScreenshotUI shoots |


### Cleared hard walls (archive)


| ID  | What                                            | Bug                                                                         |
| --- | ----------------------------------------------- | --------------------------------------------------------------------------- |
| L1  | `MonitorManager` alias                          | —                                                                           |
| L2  | BackgroundActor content lease                   | —                                                                           |
| L3  | default seat / PadOsd                           | —                                                                           |
| L4  | St leaf lease vs Actor steal                    | [done](../bugs/done/2026-09-06-lease-construct-base-steals-leaf.md)         |
| U1  | libocrpc null lease id `0`                      | [done](../bugs/done/2026-09-06-gi-null-lease-id-zero.md)                    |
| L5  | `St-ScrollView.new` hang                        | [done](../bugs/done/2026-09-07-st-scrollview-new-hang.md)                   |
| L6  | Align/Bind/Snap real mint                       | [done](../bugs/done/2026-09-07-align-constraint-relay-mint.md)              |
| L7  | `ActorMeta.set_name` Gi UTF8 + `get_constraint` | [done](../bugs/done/2026-09-08-get-constraint-align-null.md) (libocrpc pin) |


### Next work (ordered)

1. Soft stall after wallpaper `Helper-BackgroundImageCache.load` (RPC stops; Idle/`Background.loaded` path).
2. **Re-prove Wayland** — ding / `startup-complete` / chrome.
3. **5e** `get_cogl_context` when Screenshot path is exercised.
4. Soft Meta bind gaps when hit (`night-light-supported`, `unsafe-mode`).

**T-030 done when:** Wayland stays up through init + overview with no hard abort, soft gaps either fixed or explicitly deferred, and we are not blocked before `startup-complete`.

---

## Purpose / architecture (context)

Replace vendored `**libshell` C** with an **owned thin host** + minimal `**Shell-16` facade** (`Shell.Global` + listed utils). Live Meta/Clutter/St stay on `**mutter-rpc`**; client holds leases. JS remains stock upstream.

```
mutter-rpc (server)                    gnome-shell-rpc (client)
────────────────────                   ────────────────────────────
real Meta / Clutter / St               libmutter-rpc / clutter-rpc / libst-rpc
                                       owned Shell-16 facade + GJS + js gresource
                                       no libshell-16.so
```

---

## Phases (execution order)


| Phase | Work                                              | Status                                                                  |
| ----- | ------------------------------------------------- | ----------------------------------------------------------------------- |
| **A** | Stop bootstrap noops; mark 0.7.3 row 3 superseded | ⏳                                                                       |
| **B** | This backlog                                      | ✔️                                                                      |
| **C** | Thin host — tick T-rows for signed slice          | S0 ✔️; S1 ✔️; S2 ⏳ T-030 (utils + App icons + ThemeContext ✔️; re-prove UTF8/ding) |
| **D** | JS gresource without libshell (T-024)             | ✔️ (under `src/shell-js/`)                                              |
| **E** | Nested init — progress = T-030 scoreboard         | Soft gaps mostly ported; Wayland re-prove + `startup-complete` left     |


**Done when:** `gnome-shell-rpc` boots JS without `libshell-16.so`; backlog is the spec; signed slice evals nested.
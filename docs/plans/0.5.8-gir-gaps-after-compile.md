# 0.5.8 — GIR gaps that came back at `libmutter-rpc` compile

Checklist: `docs/guide-to-writing-plans.md`.

**Status:** **✔️ DONE** — all five gaps wired or noop-overridden; `.so` links.

**Parent:** [`0.5.2-close-gi-stub-gaps.md`](0.5.2-close-gi-stub-gaps.md)

**Scoreboard:** Gaps **0**.

---

## Agent rule: do not change the user’s session

Same as **0.1** / **0.5** — nested tests only.

---

## Done

**🔷** `✔️` Memcpy C structs only. Wired colour / clip / `get_pointer`.

**🔷** `✔️` `Context.add_option_group` noop (with `add_option_entries`).

**🔷** `✔️` `SelectionSourceMemory.new` — deny + override + `Helper-SelectionSourceMemory.new` (`say` / export).

**🔷** `✔️` `ShapedTexture.get_image` / `WindowActor.get_image` — deny + override + helper; ARGB32 memfd → `Cairo.ImageSurface`.

**🔷** `✔️` `Display.is_pointer_emulating_sequence` — deny + override → `return false`.

**🔷** `✔️` `WaylandCompositor.get_wayland_display` — deny + override → `return null`.

---

## Exit

- **✔️** Gaps **0**; `libmutter-rpc-16.so` still links from generated stubs + overrides.

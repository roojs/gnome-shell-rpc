# 0.8 — Init completion proof, then interaction

> # ⚠️⚠️⚠️ AGENTS — READ THIS FIRST ⚠️⚠️⚠️
>
> ## DO NOT STOP for “status theatre”
>
> **Keep working.** Do not pause to narrate progress, summarize what you tried,
> or ask whether to continue after every prove / dead end / rebuild. Carry on
> until the bar moves or you hit one of the stop conditions below.
>
> ## When you MAY stop
>
> 1. **You actually need the user’s help** — a decision only they can make,
>    credentials, a machine/session you cannot reach, or explicit approval the
>    plan forbids you from assuming. Say what you need in one short ask, then
>    wait.
> 2. **OPC / libocrpc is the problem** — and only then: write a **FAIL-backed**
>    gate under `tests/call-sync-repro/` that **FAIL**s, file the **bug in
>    OLLMchat** (`docs/bugs/`), **do not edit OLLMchat code from this tree**,
>    and **stop**. Do **not** file OPC bugs under this repo’s `docs/bugs/`.
>    PASS gates → chase the **consumer**; do not stop to “report” a theory.
>
> ## Everything else
>
> File/update the tracking bug, pick the next allowed step, rebuild, prove,
> repeat. No Idle/defer/helper thrash. No layout.js ship hacks.

**User goal:** nested **`mutter-rpc` + `gnome-shell-rpc`** stays up **and** we can prove stock init finished; then chrome **responds** to pointer / keyboard (panel, overview, keybindings) — not just paints.

Checklist: `docs/guide-to-writing-plans.md`.

**Status:** **ACTIVE** — Phase **B** minimal bar **✔️** (B1–B4).
Parked UI placement next (or soft Event packing). A/stay-up ✔️.

**Context:** archived boot [`done/0.7.7-thin-shell-bootstrap.md`](done/0.7.7-thin-shell-bootstrap.md).

**🚫** Nested only · no `meson install` on live session · no vendor `js/` without approval · no OLLMchat edits from this tree.

**🚫** `GI_RPC_JS_OVERRIDE_DIR` / `src/shell-js/ui/*.js` overlays are **debug probes only** — not the production fix and not the A4 bar.

---

## Progress (2026-09-15) — honest

### Boot where we are

| Bar | State | Notes |
| --- | ----- | ----- |
| Thin host + `init.js` corridor | ✔️ | archived 0.7.7 |
| A1 `notify_ready` · A2 `READY=1` | ✔️ | prove / client debug |
| A3 / A4 (`Meta.is_restart` / prepare) | ✔️ | early-stop prove |
| A5 preferred ask/reply | ✔️ | archived preferred hang |
| **Stay-up after A4** | **✔️** | was motion NULL `base.event`; FD + event fallthrough |
| Full `startup-complete` emit | soft | not the B bar |
| Phase B (key / panel click smokes) | **B1–B4 ✔️** | focus-smoke + key-smoke fire |

**Parked chrome (user 2026-09-15 — now unblocked):** notification banner
position, toolbar/menu placement, panel bold-on-boot, messagetray ease /
`Transition::stopped` polish, spawnv argv OPC follow-ups.

### 🔷 Next (start here) — parked UI after B

Phase B minimal **✔️**. Resume parked chrome:

| # | Work | Status |
| - | ---- | ------ |
| UI | Notification / menu placement, panel bold-on-boot | **⏳** next |
| Soft | Event typelib packing on key notify | soft |
| Soft | `pointer_click` seat pick → stock St focus (not fire_button_press) | soft |

**Layout CRITICAL (2026-09-15):** GJS LM path + construct `layout_manager=null`
wire-clear — [`bugs/done/2026-09-15-gjs-layoutmanager-null-allocate.md`](../bugs/done/2026-09-15-gjs-layoutmanager-null-allocate.md) ✔️.

**B2/B4 landed (2026-09-15):**
- B2: `Helper-Actor.fire_key` + `key-smoke` Super+n → handler fired.
- B4: `focus-smoke` — `focus_manager` + click A→B moves `stage.key_focus`.

**S3 / stay-up landed (2026-09-14):**
- OPC `call_poll` `.fd` drain — gate + nest `stdout_fd>=0 buffer=yes`.
- [`bugs/done/2026-09-14-helper-actor-event-null-base-segfault.md`](../bugs/done/2026-09-14-helper-actor-event-null-base-segfault.md)
  ✔️ — motion stay-up.
- Event Hook pack typo `"tiidu"` → `"tiddu"`
  ([`bugs/done/2026-09-14-live-invoke-get-double-critical.md`](../bugs/done/2026-09-14-live-invoke-get-double-critical.md)).

| # | Work | Status |
| - | ---- | ------ |
| S3a | Gate `echo_S` argv content | ✔️ PASS |
| S3b | `layout_changed` C symbol (Vala cname) | ✔️ |
| S3c | `GnomeShellRpc-1.0` typelib for `RpcSubprocess` | ✔️ |
| S3d | `call_poll` + `Live.Buffer` fd | ✔️ PASS (OPC + nest) |
| S3 | Stay-up past READY | ✔️ |

**🚫** layout.js PRIORITY. **🚫** OPC without FAIL gate.
**🚫** Idle / emit-defer / measure-depth thrash.

### Process

1. Phase B minimal ✔️ — next parked UI placement / soft Event packing.  
2. A-bar tracking archived:
   [`bugs/done/2026-09-13-ready-not-in-5s-prove.md`](../bugs/done/2026-09-13-ready-not-in-5s-prove.md).  
3. Soft: Event typelib still stock union — B2 prove passes with null
   event args (JS handler ignores them).

---

## How we prove

1. **Look:** `./scripts/weston-gsr-session.sh` — nest stays until you close Weston.
   **Score / agent:** `./scripts/weston-gsr-prove.sh` — **~15s** nest, **5s**
   settle after READY (early-stop on A4 / `Meta.is_restart`).
2. Property / Gi GValue: `GI_META_SMOKE=property-smoke` + `src/gjs-embed/property-smoke.js`.
3. Logs: `~/.cache/gnome-shell-rpc/{org.gnome.ShellRpc,mutter-rpc}.debug.log`.
4. Mock (`gi-rpc-smoke.sh`, `tests/call-sync-repro/`) only for a **named** RPC gap.
5. **ℹ️** `GI_RPC_JS_OVERRIDE_DIR` only when deliberately bisecting — not default.

Init hooks: `GLib.debug` in `Util.util_sd_notify` (`READY=1`). Client `--debug`.

---

## Phase A — Init finished?

**🔷** Block Phase B until **A1–A5** are **✅** on one clean nested run (A6 noted only).

| # | Marker | Status | Prove | Next if red |
| - | ------ | ------ | ----- | ----------- |
| A1 | `Meta-Context.notify_ready` | ✔️ | prove / `rg notify_ready` + `replied id=` | wallpaper / [`done/0.7.7`](done/0.7.7-thin-shell-bootstrap.md) T-030 |
| A2 | `Shell.util_sd_notify` (`READY=1`) | ✔️ | `READY=1` in client debug log in 8s | — |
| A3 | `_initializeUI` tail | ✔️ | past READY into prepare / notifications | — |
| A4 | layout prepare (`Meta.is_restart`) | ✔️ | prove `prepare-started` | full `startup-complete` soft until stay-up |
| A5 | RPC quiescence | ✔️ | ENTER == REPLY done on preferred path | [`bugs/done/2026-09-10-nested-reply-mid-emit-hang.md`](../bugs/done/2026-09-10-nested-reply-mid-emit-hang.md) |
| A6 | Chrome sanity (weak) | soft | panel visible enough to click later | layout relay — [`bugs/done/2026-09-09-layout-relay-call-sync-reentrancy.md`](../bugs/done/2026-09-09-layout-relay-call-sync-reentrancy.md) |
| **S** | **Stay-up after A4** | **✔️** | FD + no NULL `base.event` on motion | [`bugs/done/2026-09-14-helper-actor-event-null-base-segfault.md`](../bugs/done/2026-09-14-helper-actor-event-null-base-segfault.md) |

**A bar gate (user):** zero GNOME Shell CRITICALS before SIGSEGV chase.
Cover/Header ✔️ ([`bugs/done/2026-09-13-messagelist-cover-header-stack-critical.md`](../bugs/done/2026-09-13-messagelist-cover-header-stack-critical.md)).

**Landed earlier (not today’s A4):** EndSessionDialog `x`/`y`; GIR props without
getters; overlay mkdir `EXISTS`; generator override-body false skip.

---

## Phase B — Interaction (after A is green)

**🔷** Nested input causes **observable** shell behaviour (not only mutter motion debug).

| # | Surface | Status | Prove | Touchpoints |
| - | ------- | ------ | ----- | ------------- |
| B1 | Keybinding register | ✔️ | `key-smoke: ok` / `smoke-ok` | `GI_META_SMOKE=key-smoke` weston prove |
| B2 | Key/button delivery | ✔️ | `key-smoke: add_keybinding fired` / `ok` | `Helper-Actor.fire_key` → mutter grab |
| B3 | Pointer → panel control | ✔️ | `panel-click-smoke: ok` (relay) | `Actor.event` + `vfuncs["event"]`; Event typelib soft |
| B4 | Focus | ✔️ | `focus-smoke: ok` (A→B key_focus) | `Global.focus_manager` + `grab_key_focus` |

**Order within B:** B1 harness ✔️ → one stock chrome control (B3 or B4) or
B2 fire → re-run Phase A prove after each fix.

**🚫** Gestures, tablet pad, remote desktop — later.

**Done when (minimal B):** B1 **✅** + B3 or B4 **✅** on panel chrome.

---

## After 0.8 — deferred

| Plan | Why wait |
| ---- | -------- |
| [`1.0-run-to-end.md`](1.0-run-to-end.md) | Completeness track: Shell Vala worklist (hand C→Vala), TEMP deny burn-down, hardening, host-session candidate. |
| [`0.8.3-rpc-lid-hardening.md`](0.8.3-rpc-lid-hardening.md) | Hide/lock `rpc_lid` from GJS; **after 0.8** complete (1.0 Phase H). |
| [`0.8.4-wayland-subprocess-peer.md`](0.8.4-wayland-subprocess-peer.md) | Spawn stays on mutter; shell peer for `Gio.Subprocess` — discussion until stay-up + DING needs more. |
| [`0.9-multi-shell-series.md`](0.9-multi-shell-series.md) | Per-release RPC artifacts — packaging axis, parallel to 1.0. |

**Archived (vfunc performance track):** [`done/0.8.1-vfunc-capability-cache.md`](done/0.8.1-vfunc-capability-cache.md) (intent) · [`done/0.8.2-vfunc-hook-registry.md`](done/0.8.2-vfunc-hook-registry.md) (name-keyed registry + caps cache).

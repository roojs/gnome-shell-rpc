# 0.8 — Init completion proof, then interaction

> # ⚠️⚠️⚠️ AGENTS — READ THIS FIRST ⚠️⚠️⚠️
>
> ## DO NOT STOP for “status theatre”
>
> **Keep working.** Do not pause to narrate progress, summarize what you tried,
> or ask whether to continue after every prove / dead end / rebuild. Carry on
> until the bar moves or you hit one of the stop conditions below.
>
> ## When you MAY stop
>
> 1. **You actually need the user’s help** — a decision only they can make,
>    credentials, a machine/session you cannot reach, or explicit approval the
>    plan forbids you from assuming. Say what you need in one short ask, then
>    wait.
> 2. **OPC / libocrpc is the problem** — and only then: write a **FAIL-backed**
>    bug (gate under `tests/call-sync-repro/` that **FAIL**s, plus the bug doc),
>    **do not edit OLLMchat from this tree**, and **stop**. That is the **only**
>    OPC-related stop. PASS gates → chase the **consumer**; do not stop to
>    “report” a theory.
>
> ## Everything else
>
> File/update the tracking bug, pick the next allowed step, rebuild, prove,
> repeat. No Idle/defer/helper thrash. No layout.js ship hacks.

**User goal:** nested **`mutter-rpc` + `gnome-shell-rpc`** stays up **and** we can prove stock init finished; then chrome **responds** to pointer / keyboard (panel, overview, keybindings) — not just paints.

Checklist: `docs/guide-to-writing-plans.md`.

**Status:** **ACTIVE** — Phase **A** then **B** (do not start B until A bar is green).

**Context:** [`README.md`](README.md) · archived boot [`done/0.7.7-thin-shell-bootstrap.md`](done/0.7.7-thin-shell-bootstrap.md).

**🚫** Nested only · no `meson install` on live session · no vendor `js/` without approval · no OLLMchat edits from this tree.

**🚫** `GI_RPC_JS_OVERRIDE_DIR` / `src/shell-js/ui/*.js` overlays are **debug probes only** — not the production fix and not the A4 bar.

---

## Progress (2026-09-12) — honest

**User-visible / A4 bar:** **unchanged.** Stock nested session still does **not** reach
`Meta.is_restart` / layout `startup-complete`. Looking at the nest still feels
like the same stuck-after-boot behaviour.

| Item | State |
| ---- | ----- |
| A1 `notify_ready` · A2 `READY=1` · A5 ENTER==REPLY | ✔️ still |
| EndSessionDialog `No property x` · GIR prop bind spam | ✔️ still |
| **A3 / A4** | **❌ still red** — tracking [`bugs/2026-09-12-after-ready-open-preferred-emit.md`](../bugs/2026-09-12-after-ready-open-preferred-emit.md) |
| Phase B | not started |

### Tracking bug (read this before hacking)

**[`docs/bugs/2026-09-12-after-ready-open-preferred-emit.md`](../bugs/2026-09-12-after-ready-open-preferred-emit.md)** — post-READY open
`preferred_width` emit (hook 581), no client Invoke; OPC gates **PASS**; rejected
defer/Idle/`run_emit`. One hypothesis at a time; **🚫** OPC without FAIL.

### What we actually did today (not A4 green)

- **OPC gates:** `after-reply-gate` + `buffer-invoke-gate` → **PASS**. No upstream filing.
- **`Helper.Actor`:** clear `reply_args` before `emit`. Hygiene only.
- **`BackgroundImageCache` / Selection*:** dropped nested `MainLoop` (async /
  signal reply). Unproven for A4.
- **Reverted:** mid-emit Request defer, Idle flush, Connection emit helpers.
- **Scripts:** `weston-gsr-session.sh` = look; `weston-gsr-prove.sh` = agent score.

### A4 wall

Leading **symptom** (older probe): stock `PRIORITY_LOW` prepare idle never runs.
Leading **hang shape** (2026-09-12 prove): READY → panel preferred_height
chain=null → server path=base → open preferred_width emit, client silent. See bug.

**Next:** follow the bug’s allowed next steps — not shotgun Connection changes.
**🚫** layout.js override as ship path. **🚫** OPC without a FAIL gate.

---

## How we prove

1. **Look:** `./scripts/weston-gsr-session.sh` — nest stays until you close Weston.
   **Score / agent:** `./scripts/weston-gsr-prove.sh` — **~5s** + early-stop on
   `READY=1` / A4 (`Meta.is_restart`).
2. Property / Gi GValue: `GI_META_SMOKE=property-smoke` + `src/gjs-embed/property-smoke.js`.
3. Logs: `~/.cache/gnome-shell-rpc/{org.gnome.ShellRpc,mutter-rpc}.debug.log`.
4. Mock (`gi-rpc-smoke.sh`, `tests/call-sync-repro/`) only for a **named** RPC gap.
5. **ℹ️** `GI_RPC_JS_OVERRIDE_DIR` only when deliberately bisecting — not default.

Init hooks: `GLib.debug` in `Util.util_sd_notify` (`READY=1`). Client `--debug`.

---

## Phase A — Init finished?

**🔷** Block Phase B until **A1–A5** are **✅** on one clean nested run (A6 noted only).

| # | Marker | Status | Prove | Next if red |
| - | ------ | ------ | ----- | ----------- |
| A1 | `Meta-Context.notify_ready` | ✔️ | prove / `rg notify_ready` + `replied id=` | wallpaper / [`done/0.7.7`](done/0.7.7-thin-shell-bootstrap.md) T-030 |
| A2 | `Shell.util_sd_notify` (`READY=1`) | ✔️ | `READY=1` in client debug log | main.js idle not reached |
| A3 | `_initializeUI` tail | ⏳ | no stall after A1; bind CRITICAL cleared | message-list Cover/Header asserts — A noise |
| A4 | layout `startup-complete` | ❌ | `Meta.is_restart` / prepare end on stock path | [`bugs/2026-09-12-after-ready-open-preferred-emit.md`](../bugs/2026-09-12-after-ready-open-preferred-emit.md) |
| A5 | RPC quiescence | ✔️ | ENTER == REPLY done; no `socket closed` | [`bugs/done/2026-09-10-nested-reply-mid-emit-hang.md`](../bugs/done/2026-09-10-nested-reply-mid-emit-hang.md) |
| A6 | Chrome sanity (weak) | soft | panel visible enough to click later | layout relay — [`bugs/done/2026-09-09-layout-relay-call-sync-reentrancy.md`](../bugs/done/2026-09-09-layout-relay-call-sync-reentrancy.md) |

**A noise (not bar):** message-list Cover/Header stack asserts (still firing after READY).

**Landed earlier (not today’s A4):** EndSessionDialog `x`/`y`; GIR props without
getters; overlay mkdir `EXISTS`; generator override-body false skip.

---

## Phase B — Interaction (after A is green)

**🔷** Nested input causes **observable** shell behaviour (not only mutter motion debug).

| # | Surface | Status | Prove | Touchpoints |
| - | ------- | ------ | ----- | ------------- |
| B1 | Keybinding register | ⏳ | nested `key-smoke.js` handler logs | `src/rpc/helper/Display.vala`, `src/meta-mini/Display.vala`, `src/gjs-embed/key-smoke.js` |
| B2 | Key/button delivery | ⏳ | handler runs on key press | Mutter → Meta binding path |
| B3 | Pointer → panel control | ⏳ | one panel button does something | `ClickAction` stubs, server hit-test |
| B4 | Focus | ⏳ | focus moves on click | `Global.focus_manager` |

**Order within B:** B1 harness → one stock chrome control (B3 or B4) → re-run Phase A prove after each fix.

**🚫** Gestures, tablet pad, remote desktop — later.

**Done when (minimal B):** B1 **✅** + B3 or B4 **✅** on panel chrome.

# 0.8 — Init completion proof, then interaction

**User goal:** nested **`mutter-rpc` + `gnome-shell-rpc`** stays up **and** we can prove stock init finished; then chrome **responds** to pointer / keyboard (panel, overview, keybindings) — not just paints.

Checklist: `docs/guide-to-writing-plans.md`.

**Status:** **ACTIVE** — Phase **A** then **B** (do not start B until A bar is green).

**Context:** [`README.md`](README.md) · archived boot [`done/0.7.7-thin-shell-bootstrap.md`](done/0.7.7-thin-shell-bootstrap.md).

**🚫** Nested only · no `meson install` on live session · no vendor `js/` without approval · no OLLMchat edits from this tree.

---

## Progress (2026-09-11)

| Item | State |
| ---- | ----- |
| A1 `notify_ready` · A2 `READY=1` · A5 RPC quiescence | ✔️ on `./scripts/nested-init-prove.sh` |
| GIR props without getters (`night-light-supported`, `unsafe-mode`) | ✔️ stub overrides + Gi `get_property` / `set_property` (libocrpc GValue → `retval`) |
| Property wire prove | ✔️ `GI_META_SMOKE=property-smoke` → both reads ok (`false` nested) |
| Bind CRITICAL spam (`has no property called…`) | ✔️ gone on nested prove |
| A3 / A4 | ⏳ still red — `startup-complete` not seen; message-list Cover/Header asserts remain the noisy wall |

**Next for A:** diagnose post-READY stall (message-list layout asserts / whatever blocks `global.emit('startup-complete')`) — not more Meta property Helpers.

---

## How we prove (every change)

1. `./scripts/nested-init-prove.sh` (or same greps manually) — **30–60s** nested.
2. Property / Gi GValue: `GI_META_SMOKE=property-smoke` + `src/gjs-embed/property-smoke.js`.
3. Logs: `~/.cache/gnome-shell-rpc/{org.gnome.ShellRpc,mutter-rpc}.debug.log`.
4. Mock (`gi-rpc-smoke.sh`, `tests/call-sync-repro/`) only when a **named** RPC gap appears; then live re-prove.

Init hooks (already landed): `GLib.debug` in `Util.util_sd_notify` (`READY=1`), `Global.startup_complete` (`startup-complete`); client spawned with `--debug`.

---

## Phase A — Init finished?

**🔷** Block Phase B until **A1–A5** are **✅** on one clean nested run (A6 noted only).

| # | Marker | Status | Prove | Next if red |
| - | ------ | ------ | ----- | ----------- |
| A1 | `Meta-Context.notify_ready` | ✔️ | prove script / `rg notify_ready` + matching `replied id=` | wallpaper / [`done/0.7.7`](done/0.7.7-thin-shell-bootstrap.md) T-030 |
| A2 | `Shell.util_sd_notify` (`READY=1`) | ✔️ | `READY=1` in client debug log | main.js idle not reached |
| A3 | `_initializeUI` tail | ⏳ | no stall after A1; bind CRITICAL spam **cleared** (GIR props) | message-list Cover/Header asserts — see A noise |
| A4 | `startup-complete` | ⏳ | `startup-complete` once in client log | same wall as A3 — not the Meta property gap ([`bugs/…`](../bugs/2026-09-11-gir-property-no-getter-missing-on-stub.md) corridor **proven**) |
| A5 | RPC quiescence | ✔️ | ENTER == REPLY done; no `socket closed` | [`bugs/done/2026-09-10-nested-reply-mid-emit-hang.md`](../bugs/done/2026-09-10-nested-reply-mid-emit-hang.md) |
| A6 | Chrome sanity (weak) | soft | panel visible enough to click later | layout relay if A4 ok but geometry wrong — [`bugs/done/2026-09-09-layout-relay-call-sync-reentrancy.md`](../bugs/done/2026-09-09-layout-relay-call-sync-reentrancy.md) |

**A noise (not bar):** message-list Cover/Header stack asserts (still firing after READY).

**Landed for A3 property gap:** `MonitorManager.override.vala` / `Context.override.vala` → `Meta-*.get_property` / `set_property`; upstream libocrpc GValue pin + `retval` ([`OLLMchat/…/2026-09-11-FIXED-gi-gvalue-property-args.md`](file:///home/alan/gitlive/OLLMchat/docs/bugs/done/2026-09-11-FIXED-gi-gvalue-property-args.md)).

---

## Phase B — Interaction (after A is green)

**🔷** Nested input causes **observable** shell behaviour (not only mutter motion debug).

| # | Surface | Status | Prove | Touchpoints |
| - | ------- | ------ | ----- | ------------- |
| B1 | Keybinding register | ⏳ | nested `key-smoke.js` handler logs | `src/rpc/helper/Display.vala`, `src/meta-mini/Display.vala`, `src/gjs-embed/key-smoke.js` |
| B2 | Key/button delivery | ⏳ | handler runs on key press | Mutter → Meta binding path |
| B3 | Pointer → panel control | ⏳ | one panel button does something | `ClickAction` stubs, server hit-test |
| B4 | Focus | ⏳ | focus moves on click | `Global.focus_manager` |

**Order within B:** B1 harness → one stock chrome control (B3 or B4) → re-run Phase A prove after each fix.

**🚫** Gestures, tablet pad, remote desktop — later.

**Done when (minimal B):** B1 **✅** + B3 or B4 **✅** on panel chrome.

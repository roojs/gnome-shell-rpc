# 0.8.6 — Connect-driven signal subscribe

**User goal:** nested **`mutter-rpc` + `gnome-shell-rpc`** stays up and chrome responds to pointer / keyboard. This cut makes a local GJS `.connect(name)` also request `RPC-Live-Subscribe.rpc_signal` for that name, so the server registers a handler and Notifications reach the client proxy.

Checklist: `docs/guide-to-writing-plans.md`.

**Status:** DISCUSSION — supporting 0.8. Wrap lives in `src/shell-js/signals.js` (`resource:///org/gnome/shell-rpc/signals.js`). `Shell.signal_connect` / `Shell.signal_disconnect` relay to `Runtime.ensure_signal_subscribe` / `ensure_signal_unsubscribe`. Resource is compiled and registered; wrap is **not** eval'd yet. `gjs src/gjs-embed/connect-subscribe-smoke.js` PASS.

**Chosen:** wrap `GObject.Object.prototype.connect` (and `connect_after` / `connect_object`) in JS, injected from **our** GJS host before `init.js`. Not a GI `connect` on stub classes. Not `LD_PRELOAD`. Not linker `--wrap`. Not emit-time hooks.

**Docs:** [`signals-client.md`](../signals-client.md) · [`signals-server.md`](../signals-server.md)

**Unblocks:** removing the manual `ensure_signal_subscribe` call sites that exist only because GJS `.connect()` never talks to the server.

---

## Scope

**🔷** Pattern: GJS `.connect(name)` → local GObject handler → `Runtime.ensure_signal_subscribe(proxy, name)` → `RPC-Live-Subscribe.rpc_signal` → server `g_signal_connect` that name → Notification re-emitted on the client proxy.

**🚫** Vfuncs, class-struct slots, `vfunc_*`, `VfuncRelay`, `Helper-Actor.add_hook`, `Live.Hook`, `clicked` class-closure delivery.

**🚫** Forward every emission from a lease. **🚫** Subscribe at emit time (`g_signal_override_class_closure`, `g_signal_add_emission_hook`, `GSignalAccumulator`, `has_handler_pending` on dispatch). **🚫** `LD_PRELOAD`. **🚫** Linker `--wrap` of the stub `.so`. **🚫** GI method named `connect` on generated stubs (marshalling / `connect_after` never-lazy). **🚫** Vendor stock `js/` / `environment.js`. **🚫** Idle / Timeout as the subscribe flush. **🚫** OLLMchat / libocrpc edits from this tree.

---

## Why

```text
GJS .connect('stopped', handler)
  -> GObject.Object.prototype.connect (libgjs builtin)
  -> local GObject handler list
  -/> Runtime.ensure_signal_subscribe
  -/> RPC-Live-Subscribe.rpc_signal
```

GLib has no connect-time hook (`handler_insert` is static). Accumulators and emission hooks run on **emit**. The host already evals JS before `init.js` (`register-class-trace-preload.js`, `app-launch-click-probe-preload.js` in `ShellApplication.vala`). Same slot, always on.

Hand `ensure_signal_subscribe` call sites that GJS `.connect()` should replace once proved:

| Call site | Signal |
| --- | --- |
| `Meta.get_display()` | `workareas-changed` |
| `St.Entry.clutter_text` | `text-changed`, `key-focus-in`, `key-focus-out` |
| `Clutter.Actor.get_transition()` | `stopped` |
| `St.Adjustment.add_transition()` | `stopped` |

`Meta.Laters` `before-update` is Vala `signal_before_update.connect()` — this wrap does not see it. Leave that call site. Helper-Actor `Signal.lookup("clicked")` is class-closure policy; leave it.

---

## Intended vs exists

```text
intended:
  GJS obj.connect(name, handler)
    -> original GObject.prototype.connect (handler id unchanged)
    -> if leased proxy: Shell.signal_connect -> Runtime.ensure_signal_subscribe

exists:
  Shell.signal_connect / Shell.signal_disconnect in shell-gi
  signals.js gresource registered, wrap not eval'd
  GJS connect is local only
  subscribe only from hand Vala call sites
```

Server contract does not change. One wire subscription per `(connection, lease, name)` already fans out to many local handlers.

---

## Inject

**Where:** `src/shell-client/ShellApplication.vala`, after `new Gjs.Context.with_search_path(...)`, **before** `eval_module_file(init.js)`. Same slot as the existing debug preloads; this one is not env-gated.

**What:** `resource:///org/gnome/shell-rpc/signals.js` (compiled from `src/shell-js/signals.js`, extra gresource — not vendor `js-resources.gresource.xml`). Eval that file, then `Signals.install()`. Do not patch stock `environment.js`.

Wrap all three GJS builtins (they do **not** call each other):

```text
GObject.Object.prototype.connect
GObject.Object.prototype.connect_after
GObject.Object.prototype.connect_object
```

Each wrapper: call the original first (keep the handler id), then `Shell.signal_connect(this, name)` → `Runtime.ensure_signal_subscribe`. The Vala method already no-ops when `rpc_lid == 0` or the object is not a `Live.Handle`.

`Shell.signal_connect` / `Shell.signal_disconnect` are gnome-shell-rpc extras (C `shell_signal_connect` / `shell_signal_disconnect`), not stock Shell. Do not invent a second subscribe RPC.

**Do not** use `GI_RPC_JS_OVERRIDE_DIR` as the ship path. That overlay is a debug probe.

**Now:** resource compiled + registered; `Shell.signal_connect` in `src/shell-gi/Signals.vala`. Wrap eval is still off.

---

## Flow

```text
GJS obj.connect(name, handler)
  -> orig.connect (local handler id)
  -> Runtime.ensure_signal_subscribe(obj, name)
       not Live.Handle / rpc_lid == 0  -> pending or return
       already in signal_subs          -> return
       call_poll depth > 0             -> pending
       else                            -> RPC-Live-Subscribe.rpc_signal
```

```text
safe:   reply decoded -> lease assigned -> subscribe
unsafe: nested subscribe while call_poll is decoding a reply
```

Pending flush is on `register_handle` and on `do_call` returning to depth 0. Stack unwind, not `GLib.Idle`.

---

## Nested delivery (not a skip-as-design)

Current nested hacks that may still blow up if a real GJS `.connect()` starts a server subscribe. Treat as delivery problems to re-prove, not as reasons to skip the wrap:

- `style-changed` re-entry inside `show()` / setter (`local_emit_after` today).
- `key-press-event` / `key-release-event` — `Clutter.Event` not on the wire; Helper event relay emits locally.
- Helper `captured-event` already emits locally — a second subscribe would double-fire.

Do not encode a permanent skip list until a FAIL gate names one. If the wrap + `style-changed` crashes nested, that is the next bug, not a reason to revert the wrap.

---

## Runtime API

`ensure_signal_subscribe` stays the one wire entry. `ensure_signal_unsubscribe` is the matching drop (`RPC-Live-Subscribe.unsubscribe`). GJS reaches both through `Shell.signal_connect` / `Shell.signal_disconnect`.

#### Add

- JS preload + `ShellApplication` always-eval before `init.js` (**⏳** resource exists, eval off)
- `pending` map: object → set of detailed names
- `call_poll` depth in `do_call`; flush at depth 0 and from `register_handle`

Do not add a second subscribe RPC.

---

## 1. Gate — GJS `.connect()` subscribes without a hand call

**Where:** `src/shell-js/signals.js` + `src/gjs-embed/connect-subscribe-smoke.js`. Resource compiled; wrap **not** eval'd by `ShellApplication`.

**✔️** Wrap class (2026-09-23): `gjs src/gjs-embed/connect-subscribe-smoke.js` — leased `rpc_lid=7` records four `pinged` subscribes (`connect` / `connect_after` / `connect_object` / second `connect`); `rpc_lid=0` and plain `GObject.Object` do not subscribe; emit still delivers. Next: real `ensure_signal_subscribe` on a leased peer.

**🔷** The smoke must **not** call `ensure_signal_subscribe` from Vala. After the lease exists: `proxy.connect('pinged', handler)`, server `fire`, local handler runs with the payload.

Also prove:

- second `.connect` of the same name does not send a second wire subscribe
- a non-`Live.Handle` GObject `.connect()` does not RPC
- `connect_after` / `connect_object` also subscribe (they do not go through `connect`)

FAIL today is the baseline. PASS is this plan.

---

## 2. Pending until lease / until `call_poll` depth 0

**Depends on:** §1.

**🔷** Connect during construct (`rpc_lid == 0`) still attaches the local handler, then subscribes from `register_handle`.

**🔷** Connect while `do_call` is in flight queues; flush when depth returns to 0.

No Idle.

---

## 3. Delete redundant hand call sites

**Depends on:** §1 PASS, nested prove still `READY=1`.

Remove hand `ensure_signal_subscribe` only after the matching GJS `.connect()` is shown to subscribe:

- `Meta.override.vala` (`workareas-changed`)
- `Entry.override.vala` (`text-changed`, `key-focus-in`, `key-focus-out`)
- `Actor.override.vala` (`get_transition` `stopped` only — **not** `clicked` / `style-changed`)
- `Adjustment.override.vala` (`stopped`)

**🚫** Do not remove `Laters` `before-update` (Vala connect).
**🚫** Do not remove Helper-Actor `clicked` subscribe.
**🚫** Do not remove `style-changed` `local_emit_after`.

One site per nested prove. Do not batch-delete.

---

## 4. Disconnect accounting (later)

**⏳ 🔷** after §1–§3. Server already has `RPC-Live-Subscribe.unsubscribe`. Wrap `disconnect` / `disconnectObject` only when adding a handler refcount. A leaked server subscription is less wrong than unsubscribing while other local handlers remain.

---

## Prove

1. GJS connect-subscribe smoke PASS without a Vala `ensure_signal_subscribe` in the test.
2. Nested `./scripts/weston-gsr-prove.sh` still reaches current A/B / `READY=1`.
3. After deleting the Entry site: overview search still fills.
4. After deleting the Transition site: MessageTray / ease `stopped` still runs.
5. `buttonbox-hpadding-smoke` / style path must not regain the `style-changed` SIGSEGV.

---

## Out of scope

- Vfunc override detection, class-slot pointer compare, `vfunc_clicked`.
- Making `style-changed` notifications safe inside `call_poll` (chase only if the wrap FAILs nested).
- Client-to-server `emit`.
- Generic signal return transport.
- Per-type generated `connect()` methods.
- Changing libocrpc `Subscribe.vala`.
- Vala `signal_foo.connect()` (Laters stays hand-subscribed).

---

## Client doc (when implemented)

Update [`signals-client.md`](../signals-client.md):

```text
GJS .connect() / .connect_after() / .connectObject()
  -> original GObject.prototype.* 
  -> Runtime.ensure_signal_subscribe -> RPC-Live-Subscribe.rpc_signal

connect-driven subscription -> implemented (GJS wrap)
Vala .connect()             -> still local / hand subscribe
```
